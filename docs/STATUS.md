# Almanac — Status

Authoritative record of where implementation stands against
`docs/design/2026-09-01-almanac-system-design.md`. Updated **in the same
commit as the work it describes**, never as a follow-up.

## Current position

**Phase 0 (Exploration) complete — 9 of 9. Phase 1 complete — 7 of 7,
exit gate verified and merged to `main` (PR #7, `cec64dc`). Phase 2 —
7 of 9 tasks done, plan merged (PR #8, `7f7ffb4`); Tasks 8–9
**development complete** (backfill orchestration, Terraform Workflows,
Photon A/B harness). **The Azure burn is DONE — the Tier 3 backfill
completed on the seventh trigger (run `227270110474807`): 92 of 92 days,
341,060,851 Bronze rows, 165.987 GB gz, zero missing hours across 2,208
files, for $11.96 — 38% of the $31.71 estimate and 6.5% of the $184
credit.** Six prior triggers failed and were fixed (DBFS-root-disabled +
Repos-path deploy gaps, then a missing `--source-config` param, then a
Spark `fs.defaultFS` path-resolution bug, then a missing Unity Catalog
storage credential for the lake storage account, then staging on
`/local_disk0` — node-local on a multi-worker cluster, then the `file:`
URI scheme that same fix silently invalidated, since Spark must address a
Unity Catalog volume by its bare `/Volumes/...` path). The successful run
then surfaced a ninth defect of the opposite kind — it did everything
right and reported **FAILED**, because `sys.exit(main())` raises
`SystemExit(0)` and Databricks' IPython task host reads that as a failure.
**Nine defects total**, all written up in
`docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md`, which
also carries one **open, undiagnosed** observation (7 staging files
survived cleanup on the final day only). The run's two headline numbers
were then each chased to a real answer rather than quoted: throughput is
published per layer, and the whole pipeline beat the Bronze-only
calibration by **2.4x per cluster-hour**
(`2026-09-02-backfill-throughput-per-layer.md`); and **0 quarantined of
341M rows** was shown to be a fact about the data rather than an inert
ruleset, by binding the shipped rules to violation cases for the first
time — with the carve-out that `created_at_not_future` structurally cannot
discriminate on a backfill, so four rules had power, not five
(`2026-09-02-zero-quarantined.md`). The active plan is
`docs/plans/2026-09-01-phase-2-gold-and-burn-plan.md` (9 tasks: **Tasks 1–7
done** — the two Phase 1 defect fixes, the dbt scaffold, `dim_repo`'s
SCD2 snapshot, `fact_pull_request` as an event-native accumulating
snapshot, the §5.1 label + `agg_repo_daily`, enforced contracts +
referential tests in CI, then the REST-API second source as a falsifiable
gate — then the Tier 3 backfill and the Photon A/B, both on Azure). Work
is on branch `phase-2-gold`. Phase 1's plan
(`docs/plans/2026-09-01-phase-1-pipeline-plan.md`) is complete and
retained as the record of what was built.

Scaffold, tooling, CI, a containerized Spark + Delta environment, the
ingestion edge (URL construction, fetching, committed fixtures), the
declarative source contract, and the whole of Bronze → Silver exist and
are green. Silver is complete and runs end to end on both committed
fixtures: era normalization, cross-hour dedup, null-safe quality rules,
and a conserving clean/quarantine split. Measured on the fixtures —
modern 2,000 raw → 2,000 clean, 0 quarantined; legacy 2,000 raw → 1,997
clean + 3 quarantined (all `repo_id_present`, the three records carrying
no `repository` object) + 0 removed.

### Tier 3's span, derived — the number Phase 1 existed to buy

Committed here with its arithmetic, as design doc §4.5 step 4 requires,
**before** any backfill starts. Measured on Databricks job run
`404509887846902`, 2026-09-01.

```
measured  one day (2025-08-13, 24/24 hours)  = 2.012 GB gz, 3,794,323 rows
          billed cluster time                = 523.5 s = 0.14542 h
          throughput                         = 13.84 GB gz / billed cluster-hour
          cluster rate (5 VMs @ $0.474)      = $2.370 / hr
          $ per day-of-data                  = 0.14542 x 2.370 = $0.3446

derived   budget for Tier 3 = 0.40 x $184    = $73.60
          affordable days   = 73.60 / 0.3446 = 214 days
          Q3 2025                            = 92 days
          cost of the full quarter           = 92 x 0.3446 = $31.71 (17.2%)
```

**Tier 3 grows from "1 month" to the full Q3 2025 quarter** — 2025-07-01
to 2025-09-30, ~185 GB gz — at 17.2% of the credit against a 40% cap,
**2.3× headroom**. §4.5 says the rule binds in both directions; it bound
upward. The headroom is what absorbs a full-medallion run being slower
than the Bronze-only rate this was measured at.

**The recorded cluster cost was 25% low.** §4.5 had $1.896/hr for
"4 × `D4ds_v6`" — the component rates reproduce that exactly at four
nodes, but `num_workers: 4` provisions **five** VMs, four workers and a
driver. Corrected to $2.370/hr, and "97 cluster-hours on $184" is really
77.6.

### Phase 1 exit gate — verified

| Gate | Status |
|---|---|
| `make check` green | ✅ ruff, `mypy --strict`, 130 tests |
| CI green on the PR | ✅ every task's commit |
| Re-running an hour is idempotent | ✅ `replaceWhere`, tested and mutation-checked |
| Cross-hour duplicates removed (trap 4) | ✅ Task 5, tested for the first time |
| Legacy timestamps land as UTC | ✅ the `-07:00` case asserted on epochs |
| Legacy events get a stable content-hash id | ✅ and the key was corrected after it merged real events |
| Repo-name case preserved end to end | ✅ Task 4 |
| `clean + quarantined == input` | ✅ asserted as an equation with duplicates named |
| Every quality rule null-safe | ✅ `coalesce(expr, False)`, mutation-checked |
| All three schema eras ingest | ✅ both fixtures end to end; `reduced_v3` labelled |
| Missing hours reported, never filled | ✅ Task 3; the calibration reported 24/24 |
| Cluster throughput measured on Azure | ✅ findings doc committed |
| Tier 3's span derived with arithmetic | ✅ above |
| 0 clusters running, teardown verified | ✅ all five job clusters auto-terminated |
| STATUS.md updated in each task's commit | ✅ seven commits, seven rows |
| README refreshed | ✅ including the diagram when Silver landed |

**Cloud resources now exist and are running** (`terraform apply`,
2026-09-01): resource group, ADLS Gen2 with bronze/silver/gold/features
containers, and a Premium Azure Databricks workspace in **`westus3`** —
not `eastus2` as originally planned. No compute is running; the workspace
accrues no DBUs until a cluster launches. **`terraform destroy` between
sessions is the standing rule.** A subscription budget
(`almanac-credit-burndown`, $185/mo, alerts at 25/50/75/90/100% + a
forecast alert) is in force, because upgrading to pay-as-you-go **removes
the Free Trial spending limit** and the card becomes chargeable once the
credit is exhausted.

**Two measurement passes have been taken** (2026-09-01), both against
real data, and both changed the design:

1. File sizes via HTTP `Content-Length` → the tiered dataset scope, design
   doc §4.5.
2. A full parse of one real hour → the label definition, design doc §5.1.
   Formal review events reach only ~1 PR in 4, so the target was
   redefined before any code was written against the old one.

Every other figure in the docs remains bracketed or absent by design.

## Next

**Phase 3 (offline feature platform) is done — 8 of 8 tasks, exit gate
verified.** `docs/plans/2026-09-03-phase-3-feature-platform-plan.md`
executed task by task (`d25619b` spine → `9264459` as-of join → `dc92a45`
bot classifier + v1 feature groups → `bfb9e75` assembly → `97cde7c` UC
registration SQL → `159409b` runner/CLI → `09b4787` leakage suite +
runner integration test → this commit's wrap-up). Every exit-gate row
(the strict-`<` boundary, cold start never dropping a spine row, an
unclosed prior PR counted as unknown rather than not-merged, Silver-only
reads, the pinned-Delta-version reproducibility invariant, runner
idempotency) is backed by a real, currently-green test — see the
Task 1–7 verification-log rows above for what each one actually proved
and which real bugs were caught along the way (a naive-timezone
comparison, module-level `Column` constants breaking test collection,
and a whole-repo-only-visible `mypy --strict` gap in a test helper).

**Two exit-gate items are deliberately still open, not silently marked
done:**

- **`terraform destroy` has not run** (carried from Phase 2) — the
  workspace and storage account holding the real 341M-row Gold/Silver/
  Bronze quarter that Phase 3's feature tables are built from are still
  live in `westus3`, kept up on purpose since destroying now would force
  a paid re-burn to get that data back before Sep 24. A lifecycle guard
  or a separated storage state is needed before the standing destroy
  rule can apply again safely.
- **Live UC `TIMESERIES` registration is unverified against a real
  Databricks target.** `primary_key_sql`'s `DROP`/`ADD CONSTRAINT` pair
  is unit-tested as pure string-building only (`test_features_registration.py`);
  executing it for real against the still-live workspace is Phase 3's
  cloud verification step, the same Terraform/cloud-gated pattern Photon
  A/B and the backfill already used, and belongs in this file when it
  runs — not claimed here.

**Phase 4 (model + MLflow) — Tasks 1–14 done.**
`docs/plans/2026-09-03-phase-4-model-mlflow-plan.md` (Tasks 1–9) plus
`docs/plans/2026-09-04-phase-4-classification-plan.md` (Tasks 10–14) are
both fully executed. Tasks 1–8 built the `ml` dependency group, the label
join, the version-pinned training frame, the naive baseline, `train_model`,
MLflow run logging, UC registration, the runner/CLI, and the Terraform for
the training job + serving endpoint — all green, all unit/integration-
tested locally. Task 9 ran the regression path for real against the live
`westus3` workspace before the Sep 24 credit expiry and measured a genuine
null result (`model_mae` 108,890 s vs. `baseline_mae` 71,917 s), driven by
the label's severe right-skew — `docs/findings/2026-09-04-model-serving-measured.md`.
That result, plus the `closed_no_response` question §5.2 had left open,
motivated reframing to classification (§5.3, 2026-09-04): predict SLA
breach/no-breach against the measured p75 threshold (1,487 s) instead of
the raw duration. Tasks 10–13 built and tested the classification path
(`join_breach_label`, the breach-rate baseline, `train_classifier`'s
comparison sweep + PR-AUC gate, CLI/Terraform wiring); the real cloud
re-run measured a genuine **non-null** result — `LGBMClassifier` beat the
baseline by more than 2x on PR-AUC (0.612 vs. 0.285, `roc_auc` 0.828) —
and registered the winner live in Unity Catalog
(`almanac_dbx.models.pr_review_sla_risk` v1, `@champion` alias), the
**first real UC registration in this project**. That run also surfaced
and closed a real defect no local test had caught (UC refuses a model
logged with no signature), fixed and covered by a new local regression
test. Full detail in
`docs/findings/2026-09-04-classification-model-serving-measured.md`.

Real-scale running of the feature platform also surfaced and fixed three
defects the offline fixtures could not (all in the verification log,
2026-09-04): the same O(N²) bot-author skew in
`compute_author_activity`'s self-join **and** in `as_of_join` (the
feature platform's core primitive — 11.7 PiB of intermediate on the real
quarter), plus Gold's fact being a Unity Catalog managed table rather
than a Delta path on the workspace.

**The live serving endpoint is up and fully measured**, closing the item
above: `terraform apply -target=databricks_model_serving.pr_review_sla_risk`
(8m27s to `READY`), then 20 real invocations against the live URL,
validated against the model's actual signature. Warm latency: **p50
263.5 ms, p95 376.8 ms** (n=20, real `curl` timings, not CLI overhead).
**Cold start, measured for real: 51.96 s** (`HTTP 200`, correct
prediction) after a genuine ~43-minute idle gap — confirmed live that
Databricks scales to zero after 30 minutes idle
([Databricks Answers](https://answers.databricks.com/does-serverless-scale-up-down),
2026-09-04) before trusting the number. Three immediate follow-ups
confirmed the endpoint was warm again right after (0.485s/0.505s/0.308s),
matching the n=20 distribution and ruling out a fluke. Full detail,
including the two false starts the real measurement took to land (a user
query mid-wait resetting the idle clock, then a local `sleep` stalling
across a machine suspend — neither affecting the endpoint's own
server-side clock, which is what the measurement actually depends on):
`docs/findings/2026-09-04-serving-endpoint-measured.md`.

**The decision boundary was probed live, both directions, and both
verified against the model's real `predict_proba`, not assumed from the
served label alone.** Two `false` payloads (a typical human PR at
31.66%, a bot-heavy variant at 35.21%) showed the probability moving the
expected direction without crossing the endpoint's 0.5 cutoff — the same
reason §5.3 picked PR-AUC over accuracy, made visible in a live example.
A `true` was then found by reading the model's real
`feature_importances_` (`prior_pr_count` highest; `is_draft` **zero,
never split on**) and a 4,000-row random search (656/4,000 crossed 0.5,
max 74.96%) rather than by guessing — the winning combination is a real
surprise: very high prior activity paired with very low recent
engagement, not "more bot signals." Confirmed on the live endpoint
(`{"predictions": [true]}`, matching the local probability exactly). One
non-monotonic effect surfaced along the way: dropping `prior_merge_rate`
from 0.1 to exactly 0.0 *lowered* predicted risk (47.6%→35.0%), the
opposite of the naive intuition — a real interaction effect, not a bug.
Same findings doc.

The online store stays deferred per §4.4a's scope decision — the serving
endpoint it would sit behind now exists. **The vector index no longer
is**: Phase 5 built it for real (`almanac-embeddings`, 1,265,363 vectors),
so the `R`, `E` and `V` nodes on the README architecture diagram are all
`done`.

**Every real cloud job run across all four Databricks jobs — backfill,
build-features, Photon A/B, train-model — is now compiled in one place**:
`docs/findings/2026-09-04-cloud-job-run-history.md`. n = 23 real runs,
16 real defects found and fixed (none visible to `ruff`/`mypy`/the local
suite), ≈$33.50 of the $184 credit total across all four jobs.

Carried forward from Phase 1, none of it blocking:

- **A third of the calibration's billed time was single-threaded HTTP
  download**, not compute (174.3 s of 523.5 s). Parallelising the fetch is
  the obvious cost lever for the backfill.
- **The dedup window is a full shuffle keyed on `event_id`** and is the
  candidate for the dominant cost at 92 days rather than one.
- **A membership check on `event_type`** is worth having as a schema-drift
  signal, but it needs a measured list of known types and belongs at
  `warn` severity — a new GitHub event type is drift to flag, not data to
  reject.
- **The measured throughput is Bronze-only.** §4.5 step 1 describes
  bronze → silver → gold; Task 7's plan specifies Bronze. The 2.3× headroom
  is the margin, and if the full pipeline is worse than that, Tier 3
  shrinks — which is the rule working, not a failure.

## Hard dates

| Date | What |
|---|---|
| **2026-09-24** | Azure free credits ($184) expire. Phase 2's cloud burn must complete before this. See design doc §9. |

## Verification log

Each completed task adds a row here stating what was run and what the
actual result was, including failures.

| Date | Task | What was run | Result |
|---|---|---|---|
| 2026-09-01 | Design | — | Design doc written and approved. No code. |
| 2026-09-01 | Dataset scope | `curl -sI` against 5 live GH Archive files | **Measured.** 2025 hours 62.5 / 83.2 / 113.7 MB gz; 2014 hours 5.0 / 6.2 MB gz. Firehose grew ~15× since 2014. A full unsampled quarter extrapolates to ~180 GB gz — an order of magnitude above the earlier working assumption, which is what drove the tiered scope in design doc §4.5 and the rule to sample repos rather than hours. Uncompressed ratio still unmeasured. |
| 2026-09-01 | Phase 0 Task 1 — scaffold, tooling, CI | `make check` (ruff, mypy --strict, pytest) | **Green.** 3 tests pass. Installed live-checked versions: pyspark 4.2.0, delta-spark 4.4.0, chispa 0.12.0, ruff 0.16.5, mypy 2.3.1, pytest 9.1.1, pydantic 2.13.5. Three findings: (1) **delta-spark 4.4.0 declares `pyspark<=4.2.0`, and 4.2.0 is current** — the ceiling is exactly where we sit, so pyspark is capped `<4.3` or a future release breaks at SparkSession construction, not install; (2) the plan's GitHub Action versions were both wrong — `checkout` is v7.0.1 (plan said v5), `setup-uv` v10.0.1 (plan said v7), corrected from `gh api`; (3) plan ordering bug — `uv` cannot build the package without `src/almanac/__init__.py`, so the TDD "watch it fail" step could not run until the marker existed. Also found ruff 0.16.5 formats Python inside markdown, so plan docs are held to the same standard; accepted rather than excluded. |
| 2026-09-01 | Phase 0 Task 2 — local Spark + Delta | `pytest -m spark` on host; full suite in container | **Green, 6 tests, after four real defects.** (1) `delta-spark` pip ships Python bindings only — without `configure_spark_with_delta_pip` the JVM has no Delta data source and every `.format("delta")` dies with `ClassNotFoundException`; the plan configured the extensions but never arranged JAR resolution. (2) **`openjdk-17-jre-headless` no longer exists** in the Debian release backing `python:3.12-slim` (`no installation candidate`) — moved to Java 21, which Spark 4.x also supports; host stays on 17, a recorded divergence. (3) Hardcoded `JAVA_HOME` was arch-specific and wrong on one of arm64/amd64; dropped entirely since PySpark falls back to `java` on PATH. (4) No `.dockerignore` meant `COPY . .` shipped the host `.venv` into the image and host paths appeared in container tracebacks; and `uv run` at CMD re-synced without `--all-extras`, silently dropping pyspark so the container run looked like it worked but tested the wrong environment. **`array_compact` confirmed AVAILABLE** in Spark 4.2.0, closing that design doc §13 item — Phase 1's rule engine can use it. `replaceWhere` idempotency proven by test. |
| 2026-09-01 | Phase 0 CI — first real run | GitHub Actions on `ubuntu-24.04`/amd64, PR #1 | **Red, then fixed.** `Unable to resolve action astral-sh/setup-uv@v10`. Root cause is non-obvious and worth keeping: `gh api .../releases/latest` returns a **release tag**, which is not necessarily a resolvable **action ref**. `actions/checkout` publishes a floating major tag (`refs/tags/v7` exists); `astral-sh/setup-uv` does not — only `v10.0.1`. Confirmed with `git/ref/tags/<ref>` on both before changing anything. Pinned setup-uv exact. Vindicates pushing per task: this is a CI-only failure class that local `make test` and the container run structurally cannot catch. |
| 2026-09-01 | Phase 0 Task 3 — archive URLs | `pytest tests/unit/test_urls.py`, plus a live `curl -sI` on a generated URL | **Green, 9 tests.** Live check returned `HTTP/2 200`, so the scheme is confirmed against the real host rather than merely self-consistent. The unpadded-hour/padded-date asymmetry is pinned by test: zero-padding the hour would 404 ten of every twenty-four files. |
| 2026-09-01 | Phase 0 Task 4 — fetching | `make check` (ruff, mypy --strict, 29 tests) | **Green.** Four fetch outcomes distinguished and each pinned by test: 404 → ABSENT and never retried; 200-with-zero-bytes → EMPTY and never retried; 5xx/429 → FAILED and retried to the configured limit; transport error → FAILED leaving no file behind. Downloads land on `.part` and are renamed, so a crash mid-write cannot leave a truncated file a later run mistakes for complete. Ruff caught `class FetchStatus(str, Enum)` as superseded by `enum.StrEnum` on 3.11+; fixed rather than suppressed. |
| 2026-09-01 | Phase 0 Task 5 — Tier 0 fixtures | `make fixtures` against the live host; `make check` (32 tests) | **Green.** Both fixtures built at **0.76 MB**, far under the 5 MB cap. Corroborating measurement: the 2014 hour carried **17,138 events** against 2025's **227,376** — a **13.3× growth in event count**, consistent with the ~15× file-size growth measured earlier, from an independent signal. Confirmed via `git check-ignore` that fixtures are tracked (`.jsonl.gz` sidesteps the `*.json.gz` rule) and that `data/` holding 89 MB of raw downloads is not. |
| 2026-09-01 | Phase 0 Task 6 — schema-era diff | Field-path diff over 2,000 events per era; `make check` | **Green (4 tests), and design-changing.** Three findings, each measured across 2,000/2,000 events, now in `docs/findings/2026-09-01-schema-eras.md`: (1) **legacy events have no `id` at all** — 0/2,000 — invalidating the `event_id` dedup key the design was built on; resolved by a content hash for legacy with an `event_id_source` column (new §4.1a). (2) **`actor` is a bare string in legacy, an object in modern** — a type change, not the "different representation" the doc described, and legacy carries no numeric actor id at all. (3) **Legacy `created_at` carries a `-07:00` offset**, so naive UTC parsing shifts every pre-2015 timestamp seven hours, silently, past every schema check — the most dangerous property found so far given this project's premise (new §4.1b). Also corrected two wrong claims: the legacy-only event type is `TeamAddEvent`, not `DownloadEvent`/`FollowEvent`/`GistEvent`; and legacy language coverage is **better** than modern (1,712/2,000 direct on `repository.language`), the opposite of what trap 10 implied. `repo_id` is stable across both eras, so the SCD2 natural key survives. |
| 2026-09-01 | Phase 0 Task 7 — dataset measurements | 24 hourly files, 6,002,410 events parsed; `make check` (68 tests) | **Green, and the Phase 0 gate is PASSED.** **5,757 renames** across 1,234,736 distinct repos against a gate of 50 — SCD2 is decisively supported and the candidate window does not move. Renames are varied and real (ownership transfers, user renames, typo fixes), including one repo renamed **twice** inside the window, which exercises the multi-version SCD2 path. Bot share **29.0%** by `[bot]` suffix, vs 18.2% on the earlier Saturday hour — bot share is day-of-week dependent, reinforcing whole-week temporal splits. **The bot heuristic was changed on measured evidence:** its documented false positives (`robotframework`, `Abbott`) do not match the anchored rule at all, while a bare `ci$` clause matched 1,002 distinct logins of which **869 (86.7%) were human surnames**; fixed by requiring a separator, with the recall cost (`cw-circleci`) pinned by test rather than hidden. Volume-weighted inspection had shown the top 30 matches as 100% genuine bots — structurally blind, because bots are high-volume by definition. **Honest limitation:** the duplicate ratio (1 in 6.0M) does **not** test trap 4, because the sample used non-adjacent hours and never compared two consecutive ones; deferred to Phase 1. |
| 2026-09-01 | Phase 0 Task 8 — embedding sizing | 25,878 real PR/issue texts through all-MiniLM-L6-v2 on CPU | **Green. Phase 5 proceeds as designed.** 151.1 texts/sec measured. At the planned 5% repo sample a 30-day slice embeds in **0.4 h CPU** — an order of magnitude under the ~4 h threshold that would have forced a redesign. Even a 100% sample is 8.4 h, so raising the sample rate later is a scheduling question, not an architectural one. No GPU, no paid API. |
| 2026-09-01 | **A third schema era, found by question** | Bisected one hour per date from 2025-01 to 2026-08 | **Design-changing, and the most consequential finding of Phase 0.** Prompted by the user asking whether we were using 2026 data. **Between 2025-10-08 and 2025-10-15 `payload.pull_request` was cut from 48 fields to 5** — losing `merged`, `user`, `draft`, `created_at`, `title`, `body`, `additions`/`deletions`/`changed_files`. Post-October-2025 data is **structurally unable** to support the label: no merge outcome, no PR author (the label is "first response from a non-author"), no draft flag, and no text for Phase 5 to embed. Volume collapsed too — one 2026 hour carries 54,232 events vs 227,376 in 2025 (−76%), PR events −97%, review events −96%, bot share −87% — and that comparison *understates* it, since the 2026 sample is a Friday and the 2025 one a Saturday. `SchemaEra` now has a third member `REDUCED_V3`; Bronze/Silver ingest all three eras (a stronger schema-evolution story), the model is scoped to the rich era only. Documented nowhere upstream; found only because the question was asked. |
| 2026-09-01 | Window moved to Q3 2025 | Re-ran all Task 7 measurements + the label probe against 2025-07-09 / 08-13 / 09-17; verified 2025-09-30-23 still rich | **Green, conclusions unchanged.** Window is now **Q3 2025 (Jul 1 – Sep 30)** — the last full quarter before the Oct 2025 payload reduction, confirmed rich at both ends including the quarter's final hour (48 keys). Re-measured rather than quoted from a different quarter: **3,792 renames** across 1,071,901 repos (gate 50, passes), 3,798,852 events, duplicate ratio 0.000001, bot share **25.6%**. Fixtures rebuilt from `2025-08-13-14`. **New finding: case-only renames exist** (`Lumacaonta/GLB` → `Lumacaonta/glb`), so SCD2 name comparison must be case-sensitive — a case-insensitive join would silently miss them. |
| 2026-09-01 | Label broadening validated numerically | Full parse of `2025-08-13-14` (167,303 events) | **Confirms the §5.1 decision with a number.** 6,618 PRs opened; **3,550** received a review event; **6,533** received *any* human response — a **1.84× coverage gain** from the broadened label, taking coverage from ~54% to ~99% of the PR open rate. Merge rate stable across both probes (77.4% Q3 / 77.8% Q1). **Draft PRs tripled**, 2.0% → **6.8%**, making the draft-exclusion rule materially more important than when it was written. |
| 2026-09-01 | Event-native construction + REST API second source | Live test on post-change 2026 data; live GitHub REST API call | **Both verified, both adopted.** On 2026-08-28 data: event-level `created_at` present on **100%** of events and the PR author recoverable from `actor.login` on **151/151** opened events — so the primary label survives the schema break *if* facts are built from the event stream rather than the gutted embedded payload (new §4.3a). Separately confirmed the **REST API was never reduced**: `/repos/{o}/{r}/pulls/{n}` returns a **48-key** object with `merged`, `draft`, `title`, `body`, `additions`, `deletions`, `changed_files`, at 5,000 req/hr. Adopted as the framework's second source (new §4.5a), which makes the project re-runnable end to end on current data instead of a historical artifact. |
| 2026-09-01 | Phase 0 Task 9 — Azure pricing | Azure **Retail Prices API**, region `eastus2` | **Measured, and the Unity Catalog question is closed: use Premium.** Jobs Compute DBU **$0.15 Standard / $0.30 Premium** — Premium is exactly 2×. Cluster of 1 driver + 3 workers × D4ds_v5 (VM $0.226/hr each) costs **$1.354/hr Standard vs $1.804/hr Premium**, i.e. **136 vs 102 cluster-hours** on $184. Tier 3 needs an estimated 10–15, so the budget is not binding and trading governance for unused compute is a bad trade. Also **confirmed two figures design doc §8.1 had flagged as community-sourced**: Model Serving DBU is **$0.07/hr** (not ~$0.08) and the per-launch charge is **$0.07** exactly. One input remains assumed — DBU-per-node — with a sensitivity table; the decision holds across the whole range. |
| 2026-09-01 | Phase 0 Task 9 — **blocked, quota** | `az vm list-usage --location eastus2`; `terraform validate` + `plan` | **Terraform validated and plans cleanly (8 resources), but NOT applied.** The subscription is an Azure **Free Trial**, capped at **4 Total Regional vCPUs** in eastus2. The intended cluster needs **16**; even 1-driver-1-worker needs 8. Caught before `apply`, which matters: the workspace would have created successfully and then *every cluster launch* would have failed on quota, surfacing as a confusing Databricks error rather than an obvious billing one. **Decision: upgrade to pay-as-you-go** — the only path where the $184 actually funds the Spark compute it was earmarked for. Upgrading is a portal billing action with no supported `az` CLI equivalent, so it is the user's step. Task 9 resumes once the regional vCPU limit is confirmed lifted. |
| 2026-09-01 | Label validity probe | Downloaded and fully parsed `2025-03-15-14.json.gz` (227,376 events) | **Design-changing.** Expansion **7.17×** (83 MB gz → 597 MB). 6,352 PRs opened/hour, 6,015 closed (77.8% merged), but only **1,574 distinct PRs received a review event** — formal review reaches ~1 PR in 4, so "time to first review" was undefined for most of the population. Label redefined to *time to first human response* (§5.1). Also: bots are **18.2%** of events and 2,855 of PR events, drafts 2.0%, and the probed hour was a Saturday — forcing whole-week temporal splits. |
| 2026-09-01 | `.claude/` hooks | Ran both hooks against 3 constructed scenarios | **Defect found and fixed.** Both hooks read the git index at `PreToolUse` time, so `git add -A && git commit` as one command left the index empty and neither hook fired — silently, on all three of this repo's first commits. Patched to fall back to the working tree when the command also stages. Re-verified: clean tree → silent; STATUS.md+code → story-bank fires, STATUS check silent; code-only → STATUS check warns. |
| 2026-09-01 | Phase 0 Task 9 — **complete; infra live in `westus3`** | `az vm list-skus --all` across 4 regions; live workspace `/api/2.0/clusters/list-node-types`; `terraform apply`; Retail Prices API | **Applied, 8 resources, and the region and node type both changed on measured evidence.** The pay-as-you-go upgrade lifted the regional cap 4 → 10, which then exposed a larger blocker quota had been masking: **in `eastus2` and `eastus`, every node type in Databricks' Azure reference is `NotAvailableForSubscription`** (Location- *and* Zone-scoped, all 3 zones) — a cluster there could not launch at any quota. `centralus` and `westus3` are unrestricted, so the restriction is **per-region, not subscription-wide**. Moved to `westus3`: identical to `eastus2` on every measured rate (VM $0.2260, Jobs DBU $0.30, Inferencing DBU $0.07) where `centralus` is ~13% higher. **A third gate then ruled out the planned `D4ds_v5`:** availability and quota are independent, and the `DDSv5` family has a **zero allocation self-service cannot raise** (`QuotaNotAvailableForResource` — there is nothing to raise). Computed the three-way intersection instead — in the workspace's live 337-entry node catalog **and** unrestricted in `westus3` **and** family quota > 0 — yielding 37 candidates; chose **`Standard_D4ds_v6`**, same 4-core/16 GB shape, at **$0.2490/hr (+10.2%)**, so a 4-node Premium cluster is **$1.896/hr → 97 hours on $184** (was 102). Still not binding against a 10–15 hour need. Quota granted and verified against ground truth, not the request response: **regional 86** (asked 48), **Ddsv6 48**. Spot left at 3, unraised, noted. Workspace API also **confirmed the doc inference**: `D4ds_v5` is offered, `D4ds_v7` is absent (only 12 v7 types exist in 337). **Two corrections forced:** (1) an interim conclusion that the SKU restriction was subscription-wide generalized from 2 regions and was wrong — same shape as the volume claim corrected in `947935f`, and it nearly triggered a serverless rewrite of §8.1; (2) Standard-tier workspaces were **discontinued 2026-04-01**, so Premium is *forced*, not the trade-off the pricing doc recorded — corrected in place. Also measured: **Photon carries no DBU-rate premium** ($0.30 either way). |
| 2026-09-01 | `almanac-design-decision` skill | Written against the two incidents it cites | **Added, incident-derived.** Its gate 1 exists because this repo has now made the *same* error twice: the volume claim from **one** hour, and the SKU-restriction claim from **two** regions. Both were ~90 seconds of extra measurement from being caught, and both were stated as conclusions before that measurement ran. The skill requires stating the actual `n` along the dimension being generalized over. Gates 2 and 3 codify the already-standing web-validation and where-it-gets-recorded rules rather than adding anything new. **A code-quality skill and a test-coverage skill were considered and deliberately deferred** — zero review incidents so far, and coverage is already enforced mechanically by CI; both are recorded in CLAUDE.md's deferred list with the trigger that would earn them. |
| 2026-09-01 | Credit re-scope — 4 design changes | Live vendor-doc validation (Photon coverage, vector-index practice); Retail Prices API; Phase 0 Task 8 embedding measurements | **Written up before building, per `almanac-design-decision`.** The plan spent ~$25–40 of $184; four additions absorb the surplus, each converting an assertion into a measurement. **§4.5 — Tier 3 is now calibration-derived, not a fixed "1 month".** Its span was a number picked before anything ran on Spark; the procedure is now fixed in advance (calibrate on one day → derive GB/cluster-hour → take the largest contiguous slice costing ≤40% of remaining credit → commit the arithmetic to STATUS.md first). The rule binds both ways: shrinking is not a failure, quoting an unrun span is. Also corrected a small arithmetic error — Q3 is **2,208** hourly files (92 days), not 2,160, so ~190 GB gz not ~180. **§8.2 — Photon A/B, designed to permit a null result.** Validation found Photon has **only partial JSON-parsing coverage and no UDF support**, falling back to Spark while still consuming the higher DBU rate — and bronze ingest is almost entirely JSON parsing. Hypothesis recorded *before* the run: helps silver/gold, little or nothing on bronze, blended result possibly a wash. Measured per-layer, since a blended number would hide exactly that effect. **§8.3 — retrieval index sized before chosen.** From Task 8's measured 151.1 texts/sec: ~218k vectors at the 5% sample, ~4.6M at 100% — **both under the ~10M threshold where 2026 practice favours pgvector/FAISS over a managed service**, so scale does *not* settle it. Decision rule fixed instead on point-in-time metadata filtering (the §2 claim applied to retrieval), rebuild cost over query latency, and UC lineage; falls back to FAISS if managed cost exceeds ~10% of credit. **§8.1 — a live endpoint, not a described one**, plus two community figures corrected: Model Serving DBU is **$0.07, not ~$0.08** (~14% off) and launch charge $0.07 exactly; cold start remains unmeasured and explicitly unquotable. **§13's register was stale** — all nine original items were measured but still unchecked; closed each with its measurement and added six genuinely open ones, the largest being **cluster throughput, on which every dollar figure depends**. Also carried forward honestly that Phase 0's duplicate-rate measurement used non-adjacent hours and so never tested trap 4. |
| 2026-09-01 | README / CLAUDE.md / gist refresh | Read each against the actual repo state | **A staleness defect found, and the rule that missed it replaced.** The README still read *"design approved, implementation not started — nothing below is built yet"* after **all nine Phase 0 tasks**, green CI, 69 tests, and live cloud infrastructure. Two conventions were already in force (README diagram updates same-commit; README never claims something is built when it is not) and **neither caught it**, because both are phrased as *don't let it become wrong* — and the README never became wrong, it just stopped being updated. Replaced with a *positive* check asked at every stopping point: "what changed today that a reader of this file would want to know?", which a stale file fails where "is it still accurate?" passes trivially. README rewritten with real status, a Mermaid architecture diagram marking built-vs-designed, and the seven design-changing Phase 0 measurements. **Separately, the story-bank gist was found corrupted** — its description line was duplicated four times into the top of the file body by an earlier update; stripped, and the true content fetched via `gh api` rather than `gh gist view --raw`, which conceals the difference. Stories 9–11 added (the sample-of-two retraction, the three-gate quota debug, the pre-registered Photon hypothesis), plus 5 tradeoff rows and 13 index entries. Gist verified still `public: false`. |
| 2026-09-01 | §9 resequenced + Phase 1 plan written | Read §9 against Phase 0's findings and the Sep 24 credit expiry | **Two contradictions fixed, one of them mine from earlier today.** (1) §9's phase table still described Tier 3 as "one unsampled month, ~62 GB gz" while §4.5 had been changed hours earlier to make it calibration-derived — two sections of one document disagreeing. (2) **I had written that four items "absorb the $184 surplus", but two of them are scheduled after the credit expires** — live serving is Phase 4 (Oct 5–18) and retrieval is Phase 5 (Oct 19 – Nov 1), against a Sep 24 expiry. Corrected to match §9's original and correct strategy: the credit funds the data-platform proof (calibration, Tier 3, Photon A/B, and ~60% held back for re-runs); serving and retrieval run on a bounded paid window afterwards. **Resequenced around the deadline:** the old schedule put the Azure burn at Sep 15–24 — the last ten days, zero margin — against a Phase 0 record of four defects in one task, three CI failures, and a region that had to change. Phase 0 finished Sep 1 rather than Sep 7, and that slack is spent on the deadline rather than on getting ahead elsewhere. **Key structural change: calibration moves out of Phase 2 into Phase 1**, because it needs only working Bronze and one day of data, not Gold. Phase 2 now runs Sep 9–20, leaving four deliberate days of slack before expiry, and only Phases 1–2 are deadline-bound. **Phase 1 plan written** (7 TDD tasks, full test and implementation code, exit gate, and an explicit deferred list). Two blocks in its first draft were placeholder-shaped — a `split` implementation that contradicted its own test, and an inert byte counter in the calibration script — both rewritten to be correct rather than shipped with an implementer note excusing them. |
| 2026-09-01 | Phase 1 Task 1 — declarative source config | `make check` (ruff, ruff format, mypy --strict, 74 tests) | **Green, 74 tests (was 69), after two plan gaps.** (1) **`pyyaml` was never a dependency** — Step 4's `import yaml` could not have run as written, and `mypy --strict` additionally needs `types-pyyaml`; both added at live-checked floors (6.0.3 / 6.0.12.20260815), not versions carried over from the plan. Same class as Phase 0 Task 1's ordering bug: the plan specified code without the environment that lets it execute. (2) **The plan's four tests do not pin the `_rule_names_unique` validator** — every one reads the real config, which has unique names by construction, so the validator is never reached. Confirmed by mutation rather than asserted: deleting the validator left all four green. Added a fifth test that constructs a duplicate-name config, so the invariant the `_failed_rules` array depends on (a failure must be traceable to the rule that caused it) is actually enforced. **Flagged, not changed:** the rule named `event_type_known` only asserts `event_type IS NOT NULL`, which is *present*, not *known* — that name will surface in quarantine analysis, so it is worth revisiting at Task 6 when the rules are first executed.
| 2026-09-01 | Phase 1 Task 2 — Bronze ingest | `make check` (ruff, ruff format, mypy --strict, 81 tests); a 3-timezone probe of PySpark's datetime conversion | **Green, 81 tests (was 74), and design-changing — a new timestamp trap, sibling to §4.1b but on the write side.** The added value-assertion on `ingested_at` failed on the first run: 12:00Z read back as 08:00 on a UTC-4 driver. Diagnosed before fixing, and the diagnosis inverted the conclusion — `unix_timestamp()` showed the **stored instant was correct** (epoch 1788350400); it was the *assertion* that was wrong, because `.first()` returns a naive datetime in **driver-local** time. **`spark.sql.session.timeZone=UTC` governs neither direction of Python↔Spark datetime conversion.** Probed across three driver timezones rather than concluding from the one in front of me — the n=1 error `almanac-design-decision` was written for: an **aware** datetime stores correctly everywhere, a **naive** one is read as local wall time and lands off by the driver's offset (+4h `America/New_York`, −5h30 `Asia/Kolkata`, **0s on UTC**). The UTC row is the finding: **every defect of this class is green on a UTC CI runner** and red only on a laptop, so CI is structurally blind to it. Now enforced in code, not prose — `add_ingestion_metadata` rejects a naive datetime (`datetime.now()` is naive, so the wrong call is the default one), and no test asserts on a collected datetime; comparisons are on epochs. Design doc §4.1b extended with the measured table. **Three further plan defects, all found by executing the plan rather than re-reading it:** (1) the planned tests leave `tmp_path` unannotated, which `mypy --strict` rejects under `disallow_incomplete_defs`; (2) `out.first()["col"]` does not typecheck — `first()` is `Row | None` — fixed with a local `one()` helper; (3) `from pyspark.sql import functions as F`, which the plan itself specifies, trips ruff `N812`; ignored repo-wide with rationale rather than `noqa`d in every pipeline module to come. Idempotency verified by mutation as well as by test: swapping `F.lit(ingested_at)` for `F.current_timestamp()` reddens exactly the one new test and leaves the other six green.
| 2026-09-01 | Phase 1 Task 3 — hour-gap detection | `make check` (ruff, ruff format, mypy --strict, 91 tests); three mutations run against the planned tests | **Green, 91 tests (was 81), after the planned tests were shown not to pin the behaviour they name.** Two mutations of the planned implementation survived the planned suite: (1) deleting `sorted()` from `missing_hours` — `test_result_is_sorted` fed an already-ascending `expected`, so a plain filter came out sorted by accident and the assertion `got == sorted(got)` could not fail; (2) computing `present` as `len(present)` rather than `len(expected) - len(gaps)` — no planned test built a report whose `present` set carried an hour outside the range, so a report could claim more coverage than the range holds. Both now die: the sort test takes shuffled input and asserts an exact list, and a new test pins `present` as *expected hours that arrived* rather than *hours that exist somewhere*. **One plan defect was structural, not cosmetic:** Task 3 declares `gaps.py` consumes `hours_in_range`, but the planned module never imports it and could not have — `hours_in_range` yields `(date, hour)` pairs while `missing_hours` compares `datetime`s, so as planned the task shipped a function nothing in this repo could call with this repo's own data. Closed with `expected_hours(start, end)`, which owns that conversion in one place and returns **timezone-aware** UTC datetimes. That is Task 2's finding on the comparison side: aware and naive datetimes are never equal and never hash alike (verified, including `hash`), so one naive side turns a fully-present day into a reported outage of all 24 hours — silent, and shaped exactly like a real outage. A test exercises that failure mode directly, and making `expected_hours` naive reddens three tests. |
| 2026-09-01 | Phase 1 Task 4 — era normalization | `make check` (ruff, ruff format, mypy --strict, 103 tests); a 3-timezone probe of the legacy content hash; four mutations | **Green, 103 tests (was 91), and design-changing — the third member of the timestamp family, on the *identity* side.** The plan hashes `created_at` by casting the timestamp to a string, but casting a timestamp to string **renders it in the session timezone**. Probed before trusting it: one legacy event hashed three different ways under `UTC`, `America/New_York` and `Asia/Kolkata`. That hash is the surrogate `event_id` for every pre-2015 event — persisted, and the only id those events will ever have — so a backfill and a later incremental run under different session timezones would re-ingest **all of legacy history** as new rows with dedup unable to notice. Now hashed from the **epoch second**, which is an instant and carries no rendering. The planned `test_content_hash_is_stable_across_runs` could not have caught this: it compares two calls inside one session, so it pins determinism and says nothing about the variable that actually moves. Together with Task 2 (naive datetime → wrong instant stored) and Task 3 (naive vs aware → false 24-hour outage), that is the same root cause surfacing on the write, comparison, and identity sides in three consecutive tasks. **A second correctness fix in the same function:** `concat_ws` skips nulls, which the plan's comment credits with preventing a hash collapse — true, but on its own it also makes `("a", null, "c")` and `("a", "c", null)` render identically, silently merging two real legacy events that have no native id to fall back on. Null fields now write a sentinel and hold their position; a test constructs the colliding pair. **Three further plan defects:** (1) every planned timestamp assertion compares a *collected* datetime against an aware one, which contradicts the rule Task 2 established two days of work earlier and fails on every machine — rewritten as epoch comparisons via a new shared `tests/helpers.py` (which also required making `tests/` a package; it was not one); (2) `normalize_events` never selects the canonical columns despite the task declaring exactly which eight it produces, leaking `created_at_raw`, `actor_raw` and `id` into Silver — leaving two id columns, one null on every legacy row, which is the shape a later dedup is most likely to key on by mistake. **That is the second consecutive task whose declared interface and actual code disagreed** (Task 3 was the first), so the contract is now asserted as a test rather than stated in prose; (3) era labels were string literals rather than `SchemaEra`, despite the task declaring it consumes that enum — now taken from the enum, with a test asserting the Spark labels agree with `era_for`'s Python answer at both boundaries, from both sides. Each of the four guards was mutation-checked and each is killed by exactly one test. **`to_timestamp` was verified, not assumed, to parse the legacy `-07:00` offset** — it does. |
| 2026-09-01 | Phase 1 Task 5 — cross-hour deduplication | `make check` (ruff, ruff format, mypy --strict, 111 tests); a null-`event_id` probe; five mutations | **Green, 111 tests (was 103). §12 trap 4 is now tested rather than merely measured** — Phase 0 recorded a duplicate ratio of 1 in 6.0M and explicitly noted its sample used non-adjacent hours, so the across-the-boundary case had never been exercised. It is now, and the realistic bug that would break it (`Window.partitionBy("event_id", "event_hour")`, i.e. per-partition dedup) reddens exactly that test. **New finding, found by asking what the function does with input the pipeline does not currently produce:** `Window.partitionBy("event_id")` groups every null into one frame, so **four genuinely distinct id-less events deduplicate down to one** — measured, 4 in and 1 out. Two rows with no id cannot be shown to be the same event, so collapsing them is deletion, not deduplication. Silver's declared order is normalize → dedup → quality split, which puts the `event_id_present` *reject* rule — a rule that exists in the Task 1 config, so the project already treats a null id as realistic input — strictly too late to protect against it; it would report a single bad record where the rest had already been destroyed, under-stating the damage by exactly its own size. `normalize_events` structurally cannot emit a null id today, and that is precisely what made this worth pinning: the invariant was nowhere stated and nowhere tested. Id-less rows now pass through untouched. **Four plan defects:** (1) `test_earliest_occurrence_is_the_one_kept` asserts on a *collected* datetime — the third consecutive task in which Task 2's epoch rule had to be re-applied to planned test code; (2) the planned test helper is annotated `-> "DataFrame"` with a `# noqa: F821` suppressing a name that is simply never imported, and `data: list[tuple]` fails `mypy --strict`'s `disallow_any_generics`; (3) `duplicate_stats(df) -> Row | Any` collapses to plain `Any`, silently disabling type checking at every call site — narrowed to `Row`; (4) the `ingested_at` tiebreak the docstring documents had no test, so removing it left the suite green. Five mutations run, each killed by exactly one test. **Two observations carried forward rather than acted on:** `duplicate_stats` returns a bare `Row` while Task 3's sibling report type is a frozen dataclass (`GapReport`) — inconsistent, but the plan declares `Row` and nothing consumes it yet; and the dedup window is a full shuffle keyed on `event_id`, plausibly the dominant cost in Task 7's calibration run, which is a thing to measure there rather than estimate here. |
| 2026-09-01 | Phase 1 Task 6 — quality rules, quarantine split, and the Bronze→Silver runner | `make check` (ruff, ruff format, mypy --strict, 130 tests); both committed fixtures run end to end; a collision probe against a real legacy hour; five mutations | **Green, 130 tests (was 111). Silver is complete, and running real data through it found that the pipeline was silently deleting real events.** Legacy events carry no native id, so Task 4 hashes `(created_at, actor_login, repo_id, event_type)` to make one — a field set the plan describes as "the minimal set that distinguishes two genuinely different events". Measured against a real legacy hour: **two colliding pairs in 2,000 events, and neither pair was a duplicate.** One actor pushed two different commit ranges to one repo inside the same second; another opened issues #13232 and #13229 inside the same second. Dedup deleted one of each — roughly **1 in 1,000 legacy events, silently**. Phase 0's measured duplicate ratio of 1 in 6.0M could not have caught it: that sample was modern data, which carries native ids and never reaches this hash. `event_url` is non-null on all 2,000 sampled and takes the same hour to **zero** collisions; it is stable content (a canonical URL naming the specific commit range or issue) rather than a serialization artifact, which is the line that keeps the rest of the payload out of the key. The legacy fixture went from 1,995 clean to 1,997. **Task 4 and Task 5 could not be composed at all.** `normalize_events` did not carry `ingested_at`, which is exactly the column `deduplicate` orders on — so the Silver order the plan declares (normalize, then dedup) raised `UNRESOLVED_COLUMN` the first time anything tried it. Both tasks were green, and so was CI, because nothing had wired them together; the same omission also made `created_at_not_future` — a rule declared back in Task 1 — unrunnable, since it compares against `ingested_at`. That is the **third consecutive task with an interface gap, and the first between two already-committed modules** rather than between a plan's prose and its code. `ingested_at` is now part of the Silver contract, and a new test applies each declared rule on its own so a failure names the rule instead of failing the pipeline with one unresolved column. **The plan lists `silver.py` under "Files: Create" and never specifies it** — `run_silver` was written from the design's declared Silver order. It takes `ingested_at` as a required argument rather than reading the clock, per Task 2's finding: a clock read would make a replay of the same file produce different rows *and still pass a count-based idempotency check*. Era dispatch lives at the read boundary and is read from the parsed schema rather than the filename's date. Legacy `repo_name` is reconstructed as `owner/name` to match modern's already-qualified `owner/repo` — 1,997 of 2,000 legacy records carry both — because leaving it unqualified would make the column mean two different things either side of 2015 and Phase 2's SCD2 would read the era boundary as a mass rename of every repo that survived it; joined with `concat`, which propagates nulls, not `concat_ws`, which would emit a bare unqualified name. **Four further plan defects:** (1) the integration test opens `2025-08-13-14.jsonl.gz`, which does not exist — the fixture is `modern-2025-08-13-14.jsonl.gz`, and `conftest.py` already had session fixtures resolving both eras; (2) its test parameters are unannotated, the fourth task in a row `mypy --strict`'s `disallow_incomplete_defs` has rejected planned test code; (3) `-> "F.Column"` is not a real path — `Column` comes from `pyspark.sql`; (4) conservation is asserted as `clean + quarantined <= raw`, which dropping every row satisfies — rewritten as an equation with the removed duplicates named and added back, and the `integration` marker registered. **Task 1's open question settled:** `event_type_known` only asserted `event_type IS NOT NULL`, and that name would have appeared in quarantine analysis as though a membership check had run; renamed to `event_type_present`. Five mutations run, each killed by exactly one test. |
| 2026-09-01 | Phase 1 Task 7 — calibration on Azure | Databricks job run `404509887846902`, `westus3`; five runs, 0.436 cluster-hours, **$1.03** total; `make check` (130 tests) | **Green, and the number Phase 1 existed to buy is measured: 13.84 GB gz per billed cluster-hour.** One day (2025-08-13), 24/24 hours with no gaps, 3,794,323 rows, 2.012 GB gz — within 4% of Phase 0's independent ~2.1 GB/day estimate. **The plan's script could not have run.** Verified locally before spending anything: `write_bronze` partitions by `event_date`/`event_hour` and the planned script never adds those columns, so it fails with `DELTA_PARTITION_COLUMN_NOT_FOUND` — after paying for cluster startup. It also calls `hours_in_range` with datetimes when it takes dates and yields `(date, hour)` pairs (Task 3's defect, third appearance), and `len()` on that generator. `expected_hours` from Task 3 replaced all of it. **One measurement decision changed the answer materially:** the plan asks for a single wall-clock figure, which cannot separate a slow cluster from a slow network. Timing fetch and Spark separately gives 36.72 GB/hr by Spark time, 19.50 by wall clock, and **13.84 per *billed* cluster-hour once setup is counted** — the only one of the three that is what you actually pay for. A single-timer measurement would have understated the bill by a third. **A third of the billed time (174.3 s of 523.5 s) was single-threaded HTTP download**, which is the obvious Phase 2 cost lever and is deliberately not built now. **Four failed runs, each a fact worth recording** rather than a fix guessed at: the job launcher will not read `python_file` from a Unity Catalog volume even with `READ_VOLUME` granted (workspace files work — and the first grant fix *didn't* work, which is what said the component had been misnamed); **public DBFS root is disabled on this workspace**; and `spark.read.json` infers a different schema for different hours of the same day, so hour N's write rejects hour N+1. That last one is a data finding, not a config one, and its fix is a design correction: **Bronze now reads raw text, not parsed JSON** — parsing is a transformation and Bronze never transforms, which is exactly what Task 2's own Bronze tests already assumed with their `raw_json string` schema. Raw text also has one schema for every hour and every era. **The recorded cluster cost was 25% low:** §4.5 had $1.896/hr for "4 × `D4ds_v6`", and live Azure Retail Prices API rates reproduce that exactly at four nodes — but `num_workers: 4` provisions five VMs, four workers and a driver. Corrected to $2.370/hr. **Tier 3 derived and committed with its arithmetic above: the full Q3 2025 quarter, 92 days, $31.71, 17.2% of the credit against a 40% cap.** §4.5 says the rule binds in both directions; it bound upward, from one month to a quarter. Teardown verified: 0 active clusters. |
| 2026-09-01 | Phase 2 implementation plan written; Phase 1 merged to `main` | A dbt-on-Delta spike run for real (dbt-core 1.12.3 + dbt-spark[session] 1.11.0 + pyspark 4.2.0 + delta-spark 4.4.0); payload probes of both committed fixtures; a read of the committed Silver code against the plan's description of it | **Plan committed, and writing it found two defects in committed, CI-green Phase 1 code.** (1) **`run_silver` writes `mode("overwrite")` with no `partitionBy` and no `replaceWhere`** — every hour processed destroys every hour already written. At Tier 3's 2,208 files Silver would finish holding one hour, and a count-based check would look plausible. Invisible at a sample size of one file, which is all Silver was ever run against; Bronze got this right in Task 2. (2) **Silver reads the raw `.json.gz`, not Bronze** — `spark.read.json(source_path)`, so Bronze's output is read by nothing, contradicting CLAUDE.md's "payload stays a JSON string; parsing is per-type in Silver" and defeating the reason Bronze exists (a reprocess must not re-download ~190 GB). Both are Phase 2 Task 1, ahead of any Gold work. **The dbt decision was executed before being planned against, and the result changed the plan.** §8's stack table specifies dbt for Gold but no dbt had ever been run here. Install is clean (`dbt-spark` declares `pyspark<5.0.0,>=3.0.0`). Incremental `merge` and SCD2 snapshots both work on local `local[*]` Delta — **but only with a persistent Hive metastore, and the failure mode is silent**: with the default in-memory metastore dbt cannot see the existing relation, `is_incremental()` is false, and both models execute `CREATE OR REPLACE TABLE AS SELECT` while reporting `success=True`. The SCD2 dimension came back with three rows, all current, no closed row for the renamed repo — right shape, plausible counts, wrong table. With `enableHiveSupport()` and an on-disk Derby metastore the same run produces a real `MERGE` (1 row updated closing the old version, 2 inserted) and correct SCD2. Read from the Delta commit log's `operation` field, because the row counts looked correct in the broken run — which is why Task 2's regression test asserts on the operation, not the counts. **Fixture probes, 2,000 events per era:** `payload.number` on 149/149 modern PR events and `number` is one of the 5 fields surviving the Oct-2025 reduction, so `(repo_id, pr_number)` is a valid key in all three eras; **`PullRequestReviewEvent` is 0 of 106 in the legacy era** — the label's primary component is structurally absent pre-2015, which the design doc does not state, and which turns out to be a second independent argument for §5.1's broadened label; `draft` is null on all 106 legacy PRs, so the draft-exclusion rule is era-bound; `issue.pull_request` present on 55 of 92 modern `IssueCommentEvent` is the PR-vs-issue discriminator the label needs; `payload.pull_request` has **three** widths (36 legacy / 48 modern / 5 reduced), so payload era dispatch is three-way. **Two scope decisions taken by the user:** Gold is built in dbt as §8 states (not the hybrid or PySpark alternatives), and the REST-API second source stays in Phase 2 as §4.5a's gate says — planned as a falsifiable test of the "zero new Python" claim rather than an assumption of it. |
| 2026-09-01 | Phase 2 Task 1 — Silver reads Bronze, partitions its writes, and keeps the payload | `make check` (ruff, ruff format, mypy --strict on 40 files, 148 tests); both committed fixtures end to end; a union-schema probe of both eras; three mutations | **Green, 148 tests (was 130). Both Phase 1 defects are fixed, and executing the task corrected the plan that described it.** Silver now writes `partitionBy(event_date, event_hour)` with `replaceWhere` scoped to the day, and reads Bronze rather than `spark.read.json(source_path)`. `run_silver` lost its `ingested_at` parameter entirely — Bronze stamped it and Silver reads it back with the row, which removes a class of error rather than documenting it. **The plan's Step 2 was wrong and the fixture disproved it.** It said to derive `event_date`/`event_hour` from `created_at` because "a late-arriving event belongs to the hour it happened in". Measured on the committed legacy fixture: one archive file named hour 14 holds events from 14:05 to 15:01 at `-07:00` — **UTC 21:05 to 22:01, 1,957 events in hour 21 and 43 in hour 22**. Deriving the partition from `created_at` scatters one source file across two partitions and no hour-scoped `replaceWhere` can then replace that file's contribution idempotently. Partition by ingest, reason by event time: `created_at` stays the event-time column every fact and feature is computed from. **The grain is a day, not an hour**, decided for a reason the plan did not state: §12 trap 4 is that duplicate ids occur across hour-file boundaries, so an hour-at-a-time Silver would dedup inside each file, never see the boundary, and quietly undo Phase 1's Task 5 while every unit test stayed green. Day-boundary duplicates remain out of scope, stated rather than accidental. **Parsing uses one explicit union schema for all three eras**, with `actor` the single exception — it is a bare string before 2015 and an object after, which no struct field can express, so it is read with `get_json_object` (measured: `$.actor.login` resolves on 2,000 of 2,000 modern and 0 of 2,000 legacy). Probed before being relied on: 0 whole-struct parse failures on either fixture. **Two measured findings changed the code.** Legacy `IssueCommentEvent` carries no `issue.pull_request` at all — 0 of 194, against 55 of 92 modern — so a structural `IS NOT NULL` would report every legacy issue comment as "not on a PR", a fabricated negative that would silently drop the whole legacy era out of §5.1's label, which already has no `PullRequestReviewEvent` there. `is_pr_comment` is therefore **null in the legacy era**, and Gold can still resolve those by joining `(repo_id, pr_number)` against known PRs, since GitHub numbers issues and PRs from one per-repo sequence. `pr_draft` is likewise null rather than false pre-2015 (149 of 149 modern non-null, 0 of 106 legacy). **One of my own tests was wrong and failing it was the useful part:** the first `test_a_second_hour_does_not_destroy_the_first` wrote the same fixture to both hours and expected 4,000 rows; dedup correctly collapsed them to 2,000. That is trap 4 working, not the defect — so the test now lands disjoint halves the way two real hourly files do, and the duplicate case became its own test, the first to check cross-hour dedup through the *runner* rather than on crafted rows. **Three mutations, each killed:** reverting to `mode("overwrite")` and dropping the `read_bronze` date filter each reddened exactly the two day-scale tests (and correctly not the single-call hour test, where the defect cannot manifest); removing the era-nulling of `is_pr_comment` reddened its two tests — after one of them was corrected, because it had been passing for the wrong reason, asserting on a defaulted null where `parse_events` really emits `false`. The raw-row builder moved into `tests/helpers.py`: it was spelled out in two test modules, and adding a parser column broke whichever one was not being looked at — the same two-places-one-contract failure that cost Phase 1 a day. `ruff` gained `PLR0913` for tests, with the rationale recorded next to the existing `N812` note. |
| 2026-09-01 | Phase 2 Task 2 — dbt scaffold on Delta, and the silent-rebuild regression test | `make check` (ruff, ruff format, mypy --strict on 43 files, 150 tests); two dbt runs in two separate processes against a real Derby metastore; `make dbt` against the shipped project | **Green, 150 tests (was 148), and the defect the spike predicted was reproduced for real before it was fixed.** The scaffold was deliberately built *without* the metastore first, so the test could be watched failing for the right reason: two consecutive `dbt run` invocations produced `['CREATE OR REPLACE TABLE AS SELECT', 'CREATE OR REPLACE TABLE AS SELECT']` in the Delta commit log — dbt reporting `success=True` both times while silently rebuilding the model. Adding `enableHiveSupport()` plus an on-disk Derby URL turned the second commit into a `MERGE`. **The two runs are two separate OS processes, not two calls in one** — two in-process runs would pass on a session that merely happened to still be alive, while a real backfill runs each day in its own process. **Three corrections against the plan as written.** (1) Its test names `gold.fact_pull_request`, which Task 4 creates — a Task 2 test cannot assert on it. The defect is a property of the *target*, not of any model, so the test drives a canary model under `tests/fixtures/dbt_canary/` against the profile this repo actually ships; no dead model is left in `dbt/models/`. (2) Task 2's file list omits an entry point, but `make dbt` cannot exist without one — dbt-spark's session method calls `SparkSession.builder.enableHiveSupport().getOrCreate()`, which *reuses* the live session and cannot retrofit Delta or a metastore onto it, so the session must be built first. `src/almanac/gold/runner.py` (listed in the phase's file table, unassigned to a task) lands here as the minimal version. (3) The `databricks` target uses dbt-spark's `http` method rather than a second adapter: `SparkConnectionMethod` was inspected live and carries `HTTP`/`THRIFT`/`ODBC`/`SESSION`, so one adapter covers both targets. Its `env_var` defaults are empty on purpose — dbt renders the whole profiles file before selecting a target, so a bare `env_var` would make the local target unusable on any machine that has never touched Databricks. Versions were re-checked live rather than carried from the plan and matched it exactly (dbt-core 1.12.3, dbt-spark 1.11.0). **`dbt build` warns `Nothing to do` and flags `models.almanac.gold` as an unused config path** — correct at this point, since Gold has no models until Task 4, and stated here so it is not mistaken later for a regression. |
| 2026-09-02 | Phase 2 Task 3 — `dim_repo`, SCD2 on repo identity | `make check` (ruff, ruff format, mypy --strict, 151 tests); `make dbt` run twice cold, second run idempotent; a 3-run SCD2 lifecycle against a real Derby metastore; mutation: folded `repo_name` to `lower()` in the snapshot's own select, confirmed it reddens the case-only-rename assertion (`assert 1 == 2`) for the right reason, reverted | **Green, 151 tests (was 150), and the metastore-registration gap named at Task 2 is now built and exercised for the first time.** `dim_repo` is a `strategy='check'` snapshot on `repo_name`, keyed on `repo_id` — the field trap 7 measured stable across all three schema eras while `repo_name` is exactly what trap 8 measured changing (5,757 renames in a 24-hour sample). dbt's stock `check` strategy already generates a null-safe comparison (an explicit OR over both null-to-value and value-to-null transitions), so CLAUDE.md's `<=>` rule holds with no custom macro. Verified against a real three-run lifecycle in `tests/integration/test_gold_dim_repo.py`, three separate OS processes against one persistent warehouse/metastore: a rename closes the old row and opens exactly one current row; a case-only rename (`acme/glb` → `acme/GLB`) is detected rather than folded away; a repo renamed twice ends with three versions, not two; an unrelated control repo stays untouched throughout. `dbt/tests/assert_dim_repo_scd2.sql` asserts "exactly one current row per `repo_id`" on every `build`, not only inside this test — mutation-confirmed: temporarily lower-casing `repo_name` in the snapshot's own select made the case-only-rename assertion fail with `assert 1 == 2` (no version added at all), for exactly the reason the test exists. **A real bug found wiring the registration `sources.yml` has needed since Task 2:** `register_silver_sources` (new, `almanac/gold/sources.py`) embeds Silver's `clean`/`quarantine` paths into `CREATE TABLE ... LOCATION`, and a relative `--silver-path` does not resolve against the working directory the way a bare filesystem path would on a non-`default` schema — Spark silently nested it under `<warehouse>/silver.db/<the relative path>` instead. Registration succeeded, `SHOW TABLES IN silver` listed the table, and the first real query failed with `DELTA_TABLE_NOT_FOUND` — caught only because the mechanism was exercised against real data rather than assumed correct from the DDL reading right. Fixed by resolving both paths to absolute before they reach SQL. **Gold's `dbt build` step in CI and `make dbt` was a no-op smoke test through Task 2** (no model existed to select from anything); it is a real build now, so both first run `scripts/build_silver_fixture.py`, landing the committed fixtures through the real Bronze → Silver pipeline. Run against that real data (not just the crafted rows in the SCD2 test): 3,519 distinct repos across both eras, all current on a first build. |
| 2026-09-02 | Phase 2 Task 4 — `fact_pull_request`, event-native accumulating snapshot | `make check` (ruff, ruff format, mypy --strict, 158 tests); `make dbt` on the committed fixtures; a mutation dropping the `least()` fold; a lifecycle across separate dbt processes; real-fixture counts probed | **Green, 158 tests (was 151).** One row per `(repo_id, pr_number)`; every column derived from an event-level field — the event's own `created_at` / `actor_login` / `event_type` / `event_action` — never `payload.pull_request.*`. §4.3a is enforced mechanically by a grep guard over the **comment-stripped** SQL (`merged` / `draft` are the documented exception, carried from Silver's already-parsed `pr_merged` / `pr_draft`, era-bound and null-safe). Incremental `merge` on `(repo_id, pr_number)`; on a rerun the model folds the new batch against the stored row with `least()` / `greatest()` over a `full outer join {{ this }}`, so a later batch carrying no `opened` event cannot overwrite a real `opened_at` with null — **mutation-confirmed**: replacing `least(batch.closed_at, prior.closed_at)` with a plain `batch.closed_at` reddens exactly `test_out_of_order_open_after_close_keeps_both_timestamps` and nothing else. `last_ingested_at` is the per-row high-water mark driving the `ingested_at > max(...)` incremental filter: `ingested_at` is processing time and advances monotonically across a resumable backfill, so a strict `>` neither re-reads nor skips. `is_censored = closed_at is null`, flagged not fabricated (§5.1); Task 5 excludes these from the label. **Two test bugs, both caught by watching the run fail rather than by re-reading:** (1) the integration tests compared *collected* datetimes — the naive-datetime trap Phase 1 Task 2 already paid for (09:00 UTC read back as 05:00 on a UTC-4 driver); rewritten to project every timestamp column through `unix_timestamp(...)` and compare epochs, the rule the rest of the repo follows. (2) the §4.3a grep guard matched `payload.pull_request` **in the model's own docstring**, which names it precisely to say it is *not* read; the guard now strips `--` comments first. **Measured on the committed fixtures:** 319 distinct PRs (149 modern PR-event PRs + legacy PR-event PRs + 68 review-only); `first_review_at` populated on exactly the 68 modern review PRs and **0 legacy** — that era has no `PullRequestReviewEvent` (§4.3a's measured fact, and §5.1's second argument for the broadened label); `draft` non-null on exactly the 80 modern opened PRs; censoring rate 63.3% on this mid-stream slice. `assert_fact_pull_request_grain.sql` runs the one-row-per-PR invariant on every `build`, and `test_second_build_merges_rather_than_rebuilding` now asserts the `MERGE`-on-rerun from the Delta commit log for this model directly, covering that Phase 2 exit-gate item. |
| 2026-09-02 | Phase 2 Task 5 — the §5.1 label, `agg_repo_daily`, and the Silver + incremental-strategy changes they forced | `make check` (ruff, ruff format, mypy --strict, 166 tests); `make dbt` on the committed fixtures (19 dbt nodes); two mutations; a Java-`rlike`-vs-Python-`re` agreement check on 21 real logins | **Green, 166 tests (was 158).** The label is `time_to_first_response_seconds` on `fact_pull_request`: `first_response_at − opened_at`, where `first_response_at` is the earliest `PullRequestReviewEvent` / `PullRequestReviewCommentEvent` / PR `IssueCommentEvent` by an actor **other than the PR author**, and never before `opened_at` (§2). Author exclusion is **mutation-confirmed** — deleting the `actor_login <> author_login` filter reddens `test_label_counts_only_non_author_human_responses` because the author's own comment then counts. `label_exclusion` states *why* a PR is out of the trainable population — `author_unobserved` (no `opened` event → the exclusion itself cannot be evaluated, §5.1's stated-exclusion rule), `draft` (null-safe: a legacy null `draft` is unknown, not "not a draft", so a legacy PR still gets a label), `open_unobserved`, `right_censored` (open, no response yet), `closed_no_response` (closed, none ever) — and is null **iff** the label is non-null, which `assert_label_exclusion_accounts_for_every_row.sql` enforces on every build so no row can carry a null label with no stated reason. **`fact_pull_request`'s incremental strategy was rebuilt, not just extended.** Task 4's `least()` scalar fold cannot do author exclusion when a response lands in a batch *before* the `opened` event — the per-actor response detail it needs is not in the row. Replaced with **recompute-touched**: any PR with a new event this batch is rebuilt in full from a new `int_pr_events` view (the one place "which event types feed a PR" is decided, including the legacy `IssueCommentEvent` → known-PR resolution, since `is_pr_comment` is null pre-2015); untouched rows pass through from `{{ this }}`. Out-of-order arrival is now correct by construction and the `least()` fold is gone — mutation-confirmed: reverting `scope` to the watermark slice instead of the full `int_pr_events` join reddens `test_out_of_order_open_after_close_keeps_both_timestamps`. **`author_is_bot` is a first-class column** via a new `almanac_is_bot` dbt macro that must stay byte-identical to `measure.BOT_REGEX` (the anchored heuristic a bare `ci$` clause failed — 869 human surnames). `test_bot_macro.py` asserts the two pattern strings are identical **and** that Spark `rlike` (a Java regex engine) agrees with Python `re` on 21 real logins including `BarisYazici`, `swift-ci`, `VenlyCI`, `cw-circleci` — the engines differ and the string alone does not prove agreement. Bots are flagged and segmented on, never dropped (§5.1). **`agg_repo_daily`**: daily grain per repo on **event time**, `stars_gained` (there are no un-star events, so a running total is not derivable — §12 traps 1–2), `commits_pushed = sum(payload.size)` **never `size(commits)`** (capped at 20 — trap 3), `bot_events`, PR/fork/issue counts. Same recompute-touched incremental strategy; `assert_agg_repo_daily_grain.sql` pins the daily grain and `test_daily_grain_and_out_of_order_events_recompute_not_double_count` proves a late event corrects a prior day rather than adding to a stale total. **Silver gained `push_size` / `push_distinct_size`** — `agg_repo_daily` needs `payload.size` and Silver did not carry it; two nullable columns on the explicit parse schema, `push_distinct_size` null pre-2015 (measured: the legacy `PushEvent` payload is `{shas, size, ref, head}` only). **Measured on the committed fixtures** (one modern + one legacy hour): `fact_pull_request` = 449 PRs, and **every one carries a `label_exclusion`** — a single hour never contains a full open→response→close arc, so `time_to_first_response_seconds` is exercised only by the crafted lifecycle tests, not the fixture (author_unobserved 312, right_censored 129, draft 4, closed_no_response 4; `author_is_bot` 25; 0 label/exclusion mismatches). `agg_repo_daily` = 3,522 repo-days, 192 stars gained, 16,822 commits pushed, 433 bot events. **Still open for Task 9**: §5.1 does not yet state the era-bound label coverage (no `PullRequestReviewEvent` before 2015) or the `closed_no_response` category — deferred there per that task's Step 7. |
| 2026-09-02 | Phase 2 Task 6 — enforced contracts, referential tests, and the proof they fail the build | `make check` (ruff, ruff format, mypy --strict, 169 tests); `make dbt` on the committed fixtures (23 dbt nodes); a copied-project break-it test per mechanism | **Green, 169 tests (was 166).** `fact_pull_request` and `agg_repo_daily` carry **`contract: enforced`** with a `data_type` on every column, paired with `on_schema_change: fail` — a column added or retyped breaks the build rather than drifting in. Proven, not asserted: `test_a_wrong_output_type_fails_the_contract` copies the shipped `dbt/` project to a tmp dir, emits `repo_id` as a string there, and confirms `dbt build` fails with a contract error (the shipped project is never touched). **`relationships`** from `fact_pull_request.repo_id` and `agg_repo_daily.repo_id` to `dim_repo`: `test_a_fact_row_with_no_dim_repo_fails_relationships` drops the repo from the dimension in the copy and confirms the build reddens. Plus `not_null` on every key and non-nullable column, `accepted_values` on `int_pr_events.kind` and `fact_pull_request.label_exclusion`, `not_null` on `dim_repo.repo_id`. **`int_pr_events` is deliberately *not* contract-enforced** — it is an internal view, and `dbt_project.yml`'s blanket `+materialized: incremental` for `gold/` collides with `contract` + `on_schema_change` on a view at parse time (`Invalid value for on_schema_change: ignore`, no model named); the two consumer models the feature platform and BI actually read carry the enforced contracts, which is where enforcement earns its keep. **Two dbt gotchas, both found the slow way.** (1) `contract: enforced` on an incremental model requires `on_schema_change` set to `append_new_columns` or `fail`, and dbt-1.12 reads that pairing from the **schema.yml `config:` block** — the value in the model's `{{ config() }}` is silently ignored for that check (four failed `dbt parse` runs, error never naming a model). (2) Adding the `relationships` tests broke every per-model integration test that runs `dbt build --select <one model>`: the cross-model test's *other* parent isn't built, so it errors `TABLE_OR_VIEW_NOT_FOUND`. `--indirect-selection cautious` fixes the ones where the test is pulled in *indirectly* (via the model), but **not** `--select dim_repo+`, where the `+` selects the test as a graph descendant — `--indirect-selection` cannot exclude a directly-selected node. Dropped the `+`; the snapshot's own `assert_dim_repo_scd2` / `not_null` are still pulled in indirectly. `dbt build` has run on the fixtures in CI since Task 3, so the full contract + relationships suite runs there on every push; no workflow change was needed. |
| 2026-09-02 | Phase 2 Task 7 — the GitHub REST API second source, as a falsifiable gate | `make check` (ruff, ruff format, mypy --strict, 175 tests); the bound computed from measured Phase 0 numbers; §4.5a corrected in the same commit | **Green, 175 tests (was 169), and §4.5a's "zero new Python" claim is falsified — cleanly.** `conf/sources/github_rest.yml` (13 non-comment lines) *describes* the source completely and **nothing in the framework consumes it**: the only config field the Spark path reads is `quality_rules`, and the only fetch path is `archive_url(day, hour)` → one gzip file (`url_template` had been a declared field that nothing read). Onboarding a paginated, authenticated, rate-limited API took **~123 non-comment lines of new Python** — 29 for `AuthConfig` / `RateLimitConfig` / `RestSourceConfig` in `pipeline/source.py` (`SourceConfig` is `extra="forbid"` and its fields do not describe an API; `test_the_file_source_config_rejects_the_rest_config` pins that the shapes do not overlap), ~94 for `extract/rest.py`: `RestSession` + `fetch_pr` (Bearer auth, enrichment extraction), rate-limit backoff that waits **until the response's own `X-RateLimit-Reset`** rather than a fixed guess (`test_fetch_pr_backs_off_when_the_budget_is_exhausted`), and `Link`-header pagination as a generator (`test_list_pr_numbers_follows_the_link_header`). **The honest register statement** (`docs/findings/2026-09-02-second-source.md`, and §4.5a now corrected to match): the framework onboards a *file* source by YAML alone and needs ~120 lines of Python for an API — the claim held for the easy case and broke on the one that matters, which is more credible than an unfalsifiable claim of full generality. **The bound on the enrichment set is measured, not guessed**: from Phase 0's −97% PR-event rate on 2026 data (~200 opened PRs/hr), a 5% repo sample of one week of current data is ~1,680 requests — a third of one 5,000/hr window. **Tests never touch the network** (`httpx.MockTransport` throughout; `RestSession` carries injectable `sleep` / `now`); the client has never made a real request, and the enrichment is a `PrEnrichment` dataclass, not a Delta table joined to Gold — that wiring is Phase 3+, since the model is rich-era-only by design. |
| 2026-09-02 | Phase 2 Task 8 — orchestration for the Tier 3 burn (**dev only**) | ruff + ruff format + mypy --strict green; `terraform fmt` + `terraform validate` on `infra/terraform/`; full suite **201 green** (run `b29xva07w`) on the combined Task 8 + Task 9 tree — this commit is its Task 8 subset (**+17 tests → 192**), Task 9 adding only new modules and test files | **Green. The burn itself waits for a go-ahead.** `fetch_hours` parallelises the download Phase 1 measured as **174.3 s of 523.5 s billed** — a third — as single-threaded HTTP. `almanac.burn`: `process_day` (fetch + Bronze + Silver, one clock read for the day, timed per layer, per-day `GapReport`); `pending_days` + `BackfillCheckpoint` (one JSON marker per finished day, `.part`-then-rename, so a failure at day 60 resumes at day 60); `backfill` (the checkpointed loop, staging cleared per day, per-layer GB/cluster-hour in the report). `scripts/backfill.py` is the launch shim, `calibrate.py`-style. `infra/terraform/databricks.tf` adds the `databricks` provider (`~> 1.90`, locked **1.130.0**), `databricks_job.backfill`, and `databricks_job.photon_ab` (two job clusters, STANDARD/PHOTON — infra ahead of Task 9's harness that targets it) — job clusters only, **no `schedule`/`trigger`**, tags propagated, Photon a var, checkpoint dir a FUSE path not an `abfss://` URI (`BackfillCheckpoint` is pathlib and must outlive the cluster). `terraform validate` passes; `plan`/`apply` need `az login` + the live workspace (the `databricks_spark_version` / `databricks_current_user` data sources query its API) and are not run here. Gold stays out of the per-day loop by design — dbt builds the whole warehouse, not a day of it. **Not done (burn-time):** parallelise-vs-measure the fetch on a real cluster, the two named cost-risk watches, measure-and-publish per layer, teardown verification. |
| 2026-09-02 | Phase 2 Task 9 — the Photon A/B harness, and §5.1's era-bound label (**dev only**) | ruff + ruff format + mypy --strict green; `terraform validate`; full suite **201 green** (`b29xva07w`); `compare_arms` unit-tested incl. the null-result and hypothesis-check cases | **Green, 201 tests (was 192 at Task 8's subset). The A/B run waits for a go-ahead.** `almanac.burn.photon.compare_arms(off, on)` → per-layer speedup + verdict (`helped` / `no material change` / `hurt` / `no data`), `dbu_overhead`, `cost_off`/`cost_on`, `paid_for_itself`, and `hypothesis_holds` — §8.2's prediction *checked*, not asserted: bronze not materially helped, silver and gold are. Rates (`usd_per_dbu`, `usd_per_node_hour`, `num_nodes`) are the caller's, never hardcoded — the discipline `2026-09-01-cluster-throughput.md` follows. Arm order guarded (off first). `scripts/photon_ab.py`: `run --photon/--no-photon` measures one arm on a cluster (bronze + silver + gold, timed per layer; `dbus_consumed` filled from the billing API 24–48 h later), `compare` diffs two arm JSONs. `databricks_job.photon_ab` runs `arm_standard` and `arm_photon` on two job clusters of identical shape. **Design doc §5.1 now states the label is era-bound**: `PullRequestReviewEvent` is structurally absent pre-2015 (0 of 106 legacy PRs vs 68 modern), so a Tier 2 PR's label rests on the other two components — a second, independent argument for the broadened label; `draft` and `merged` are era-bound too (null-safe / three-way). **Not done (burn-time):** the two arm runs, publish per layer including if negative, `terraform destroy` verified empty, the three-artifact wrap-up, close §13's Photon and DBU items. |
| 2026-09-02 | Repo-wide comment/docstring sweep; `almanac-code-style` gains item 10 | `make check` (ruff, ruff format, mypy --strict, 201 tests); full suite re-run standalone after the sweep (`201 passed in 1348.10s`); every swept file's diff re-read against the new checklist item | **Green, 201 tests, unchanged — comment- and docstring-only across 59 files (~1,230 lines removed, ~420 added).** Every module's narrative docstrings and "what" comments trimmed to one-line docstrings and why-only comments, with each load-bearing "why" kept as a pointer (a design-doc §, a findings-doc name, or the STATUS row that already carries the full account) rather than restated as an inline essay. No `.py` / `.sql` / `.sh` logic touched — confirmed before the run that no `__doc__` is read at runtime (all three `argparse` descriptions are string literals) and that the two SQL guard tests (`test_no_column_reads_an_embedded_payload_object`, the §4.3a grep) strip `--`/`{# #}` before matching, so a comment-only edit cannot move them. One collapsed docstring left a dangling reference (`test_events_keep_their_source_hour_not_their_utc_hour`, "...and this is why." with no "why" left after it) — caught on re-read and rewritten to state the claim standalone rather than pointing at deleted prose. `almanac-code-style` SKILL.md gains **item 10 (comment discipline)**: no "what" comments, one-line docstrings, no tutorial prose or measured-number essays in code — that story lives in `docs/`. `code-style-reminder.sh`'s checklist line is extended to match, CLAUDE.md's tooling entry records both, and the three other `.claude/hooks/*.sh` headers get the same trim (their incident rationale already lives in CLAUDE.md's tooling section, so nothing was lost by shortening them). This pass is the "dedicated pass" item 10 names for the Phase 0–1 modules; from here the rule applies as code is written, and CLAUDE.md's scope-discipline rule still bars an opportunistic reformat of untouched code riding on an unrelated change. |
| 2026-09-02 | Phase 2 burn — deploy setup, and a real defect caught before spending anything | `terraform apply` (2 job resources created, then updated in-place for corrected paths); `databricks fs cp` probes against both the DBFS and the UC-volume path; `az vm list-usage` quota re-check | **Both job definitions live; a load-bearing default was wrong and is now fixed and committed.** `az login` + `DATABRICKS_HOST` is sufficient auth for both the `databricks` Terraform provider and the CLI — no `.databrickscfg` profile needed, confirmed by `databricks current-user me` succeeding on Azure CLI credentials alone. **`variables.tf`'s `/dbfs/FileStore/...` defaults do not work on this workspace**: `databricks fs cp` against `dbfs:/FileStore/...` returns `Error: Public DBFS root is disabled`, the same restriction Task 7's calibration run hit once as one of four failed-run causes — now confirmed to cover `FileStore` specifically, not only the DBFS root generally, and confirmed **before** any cluster ran rather than discovered mid-backfill after paying for it. Fixed with two Unity Catalog volumes, `almanac_dbx.burn.checkpoints` and `almanac_dbx.burn.photon_ab` (the alternative `backfill_checkpoint_dir`'s own comment had already named but not tried) — `/Volumes/...` is FUSE-mounted and pathlib-compatible without the restriction, verified writable by the same `databricks fs cp` probe. **A second, unrelated deploy gap**: `backfill_python_file` / `photon_ab_python_file` / `almanac_wheel` defaulted to `/Workspace/Repos/almanac/...`, which assumes a Databricks Repo linked to this repo's git remote — not configured here. Used `databricks sync` to `/Workspace/Shared/almanac/` instead (checksummed identical to the local wheel after upload) and updated the three defaults to match, deliberately avoiding a `/Workspace/Users/<email>/...` path so a personal email is not what ends up committed. `terraform plan` after both fixes: **no changes** — committed config now matches the live, verified-working state exactly, so a future `apply` cannot silently revert either fix. Azure quota re-checked live rather than assumed from Task 9's record: Ddsv6 48/48 vCPUs available, the job needs 20. **The actual backfill run has not started yet** — this row is setup only. |
| 2026-09-02 | Phase 2 burn — first real run, and two more defects `terraform validate` could not see | Triggered `databricks_job.backfill` for real (run `588210730480473`); `databricks jobs get-run-output` on the failed task | **Failed fast — `FileNotFoundError: conf/sources/gharchive.yml` — and correctly so: neither defect is a `terraform validate`-visible error, only a runtime one.** (1) **`databricks.tf`'s `backfill` task never passed `--source-config`**, so `scripts/backfill.py` fell back to its relative default, which resolves against the repo root — true for `make`/CI, false for a job task's working directory on a cluster. Fixed by adding a `source_config_workspace_path` variable, wired into both jobs' `parameters`. (2) **Reading `photon_ab.py`'s own argparse before this could bite the same way at 2x the cost**: `databricks_job.photon_ab`'s task parameters open directly with `--photon`/`--no-photon`, but `main()` dispatches on a required subcommand (`run`/`compare`) — the literal token `"run"` was never in the parameter list, which fails before any flag is even parsed. Caught by reading the script against the terraform that calls it, not by running it; fixed in the same commit as (1), one turn ahead of triggering that job for real. `terraform plan` after the fix: 2 to change (both in-place), applied clean. **Cost of the failed run**: cluster provisioned and one Python process failed within its first minute of Spark being live — a few cents against the $184 credit, not the $31.71 the full run would have cost had this failure mode instead been a silent wrong-answer one. Re-triggering now. |
| 2026-09-02 | Phase 2 burn — second real run, and a defect invisible to every local Spark session | Triggered `databricks_job.backfill` for real (run `347766258130286`); `databricks jobs get-run-output` on the failed task; `make check` (ruff, ruff format, mypy --strict, 202 tests, was 201) | **Got further — fetched a real day of data — then failed on a path that was never actually under DBFS.** `UnsupportedOperationException: Public DBFS root is disabled` on `/local_disk0/almanac-staging/2025-07-01-0.json.gz`, genuine local disk. Root cause: `_land_bronze`'s `spark.read.text(str(result.path))` (and `scripts/calibrate.py`'s identical line) passes a bare, scheme-less path, and Spark resolves a bare path against Hadoop's `fs.defaultFS` — `dbfs:/` on this cluster, `file:///` on every local Spark session this project's tests have ever run, which is exactly why nothing local could have caught it. Fixed with `.as_uri()` on both call sites, forcing the `file://` scheme regardless of the cluster's default. `scripts/build_silver_fixture.py` has the identical pattern at its one call site and was deliberately left alone — it only ever runs via `local_session()`, never on a cluster, so it was never exposed. **New regression test** sets a hostile `fs.defaultFS` (`dbfs:/nonexistent`) on the real `spark` fixture and asserts `process_day` still reads the file; its bronze/silver paths are given their own explicit `file://` scheme, matching their real `abfss://` scheme in production, so only the staging read — bare on every real run too — is exposed to the hostile default, matching production's actual blast radius rather than over-broadly breaking the test's own Delta writes. Full suite re-run standalone after the fix: **202 passed in 1312.17s**. **Both failed real runs, and the two proactive pre-run fixes, written up together**: `docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md` and the interview story bank gist (Story 25) — logged before the third trigger, at the user's explicit instruction, rather than waiting for the saga to close. Re-triggering now. |
| 2026-09-02 | Phase 2 burn — third real run, and a whole missing resource class no `terraform plan` diff could show | Triggered `databricks_job.backfill` for real (run `958902350643134`); `databricks jobs get-run-output` on the failed task; `databricks storage-credentials list` / `external-locations list` / `catalogs get almanac_dbx` to read the live Unity Catalog state; `terraform plan` (11 to add, 0 to change, 0 to destroy) then `terraform apply`, both against the reviewed plan | **Got further still — real compute ran for 88s writing Bronze — then failed on a credential that never existed.** `Invalid configuration value detected for fs.azure.account.key` writing `abfss://bronze@almanaclakekoctmh.dfs.core.windows.net/events`. Root cause, confirmed by reading the live workspace rather than guessing from the stack trace: this workspace's only Unity Catalog storage credential (`almanac_dbx`) is scoped to the metastore's own managed storage account (`dbstoragecyy2suzhxmuq4`) — what backs the UC volumes defect #1 already fixed — and was never wired to this project's own lake storage account (`azurerm_storage_account.lake`) at all, Unity Catalog or legacy key-based. **`terraform validate` and `plan` structurally could not have caught this**: every prior defect was a wrong value in a resource that existed; this one was an entire resource class — a storage credential — that was never declared, so there was no drift to diff against, only a runtime failure once a cluster actually tried the path. Fixed with new `infra/terraform/unity_catalog.tf`: an `azurerm_databricks_access_connector` (system-assigned managed identity) granted `Storage Blob Data Contributor` on the lake storage account via `azurerm_role_assignment`, feeding a `databricks_storage_credential` (Azure managed identity), with a `databricks_external_location` + `databricks_grants` (`READ_FILES`/`WRITE_FILES`/`CREATE_EXTERNAL_TABLE`, granted to the SINGLE_USER cluster's actual running identity) per medallion container — all four (bronze/silver/gold/features), not just the two currently used, so gold's turn doesn't repeat this same discovery later. This is a real, standing IAM grant on real Azure infrastructure, not an app-code change, so it was held for an explicit go-ahead beyond the burn's original one before `apply` ran — given, then applied clean: **11 added, 0 changed, 0 destroyed**, storage credential validated the role assignment could actually reach the account on first try (no propagation-delay retry needed). All six defects across all three failed real triggers written up together in `docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md`. Re-triggering now (fourth attempt; no code change since the third, so no wheel rebuild needed). |
| 2026-09-02 | Phase 2 burn — fourth real run, and a node-locality defect only a multi-worker cluster could expose | Triggered `databricks_job.backfill` for real (run `1069556816504534`); `databricks jobs get-run-output` on the failed task; `databricks volumes create` + `databricks fs cp`/`ls` probes against the new volume; `terraform plan` (0 to add, 2 to change, 0 to destroy) then `apply` | **Cleared defect #6 and ran real Spark for 45 minutes — the longest any run had survived — then failed on storage that was never shared to begin with.** `FAILED_READ_FILE.FILE_NOT_EXIST` reading `file:/local_disk0/almanac-staging/2025-07-01-0.json.gz`. Root cause: `fetch_hours()` downloads each hour's `.json.gz` via plain Python I/O in the driver process; `_land_bronze`'s `spark.read.text(result.path.as_uri())` then reads that same path back as a **distributed** Spark job, and `/local_disk0` is per-node ephemeral disk — the `medallion` job cluster runs `num_workers = 4` by design (Tasks 8-9's fetch parallelization), so any read task scheduled on a worker other than the driver had no such file. Same `/local_disk0` path as defect #5, but a different failure mode: #5 was a wrong URI scheme fixed once and for all with `.as_uri()`; this is the same correct URI pointing at storage that is fundamentally node-local on a cluster that is not, invisible in local dev/CI where `local_session()` runs single-node. Fixed with a new UC managed volume (`almanac_dbx.burn.staging`, `databricks volumes create`), same shape as defect #1's `checkpoints`/`photon_ab` volumes — FUSE-mounted identically on every node. New `backfill_staging_dir` Terraform variable wired into **both** `databricks_job.backfill` and `databricks_job.photon_ab`'s `--staging-dir` (`photon_ab.py` calls the identical `process_day()` path and its clusters also run 4 workers, so it carried the same latent bug before ever running for real). `photon_ab`'s `--warehouse`/`--metastore` deliberately left on `/local_disk0` — Gold's dbt/Derby path hasn't been exercised on this cluster yet, flagged rather than fixed speculatively. `terraform plan` after: 0 to add, 2 to change, 0 to destroy. Defect #7 of eight now written up in `docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md`. Re-triggering now (fifth attempt). |
| 2026-09-02 | Phase 2 burn — fifth real run, and a read-after-write consistency lag on a brand-new UC volume | Triggered `databricks_job.backfill` for real (run `1111960589912821`); `databricks jobs get-run-output` on the failed task; `databricks fs ls` on the staging volume directly, both immediately after failure and again minutes later; TDD: three new tests in `tests/integration/test_burn.py` injecting the real error shape at `DataFrameReader.text`; `make check` (ruff, ruff format, mypy --strict, 205 tests, was 202); `uv build` + `databricks workspace import --overwrite` to redeploy the wheel | **Cleared defect #7 and failed fast again — 83 seconds, not 45 minutes — for a reason that only exists because #7's fix worked.** `[PATH_NOT_FOUND] Path does not exist: file:/Volumes/almanac_dbx/burn/staging/2025-07-01-0.json.gz. SQLSTATE: 42K03`. Checked the volume directly after the cluster had already terminated: all 24 of the day's hourly files were present, including the one Spark said it couldn't find — same size, same `last_modified`, written in a ~12-second parallel burst. The file was never missing. **The diagnosis recorded here — that it was merely not yet visible — was wrong, and run `156595311273405` in the next row falsified it; the real cause is the `file:` URI scheme.** What follows in this row is what was believed and done at the time: `_land_bronze` processes hours in order starting at 0, so hour 0 is read essentially the instant the parallel fetch burst finishes, with zero elapsed time for the UC volume's FUSE mount to settle; every later hour gets progressively more real wall-clock time before its own read, which is presumably why only hour 0 was ever observed to fail (a freshly created volume may worsen the window; unconfirmed). Fixed with a bounded retry around the read+write for one hour (`almanac.burn.day._land_hour`, new `Settings.max_land_attempts` / `land_retry_seconds`), catching only the `PATH_NOT_FOUND` string — anything else still fails on attempt one, same discipline as `fetch_hour`'s own HTTP retries. Safe to retry the whole read+write, not just the read, because `write_bronze`'s `replaceWhere` is already idempotent per hour. **TDD, not a guessed patch redeployed blind**: three tests monkeypatch `DataFrameReader.text` to raise the exact real error shape, proving retry-past-one-failure, give-up-after-budget, and no-retry-on-an-unrelated-exception, before the fix was trusted with another paid run. `make check` green (205 tests). Wheel rebuilt and re-uploaded via `databricks workspace import --overwrite`, not `databricks sync` — `dist/.gitignore` is `*`, which sync's gitignore-awareness would have silently skipped the one file that needed to move. No Terraform change needed; `almanac_wheel`'s path is unchanged, only its content. Defect #8 of eight written up in `docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md`. Re-triggering now (sixth attempt). |
| 2026-09-02 | Phase 2 burn — seventh run succeeds, and its own timings expose a single-threaded Bronze | Read run `227270110474807`'s per-day checkpoint files from the staging volume; compared per-layer timings across a cold day and two steady-state days; read `_land_bronze` against the gzip splittability property | **The scheme fix held — day 1 completed all 24 hours, 3,748,409 rows, zero quarantined — and the run has processed 65+ days at a stable ~3.2 min/day with no degradation as the Delta tables grow.** Steady state per day: fetch ~13 s (7%), Bronze ~130 s (65%), Silver ~57 s (28%). **The anomaly: Bronze does far less work than Silver and takes 2.3x longer.** Silver parses 3.7 M JSON documents, normalizes three schema eras, shuffles for dedup, runs six quality rules and writes two tables in ~57 s; Bronze reads a file, adds two literal columns and writes, in ~130 s. Diagnosis: `_land_bronze` loops hour by hour, and a `.json.gz` is **non-splittable** — one file is one Spark input partition, one task, one core — so Bronze runs 24 single-core jobs in series while the 4-worker cluster sits largely idle. Silver is fast for the mirror-image reason: it reads Delta, which fans out across its 24 hour-partitions. **Also corrects an earlier reading of the same numbers** that took 'Bronze is expensive' to mean the write was expensive and the cluster saturated; the write is not expensive, the read is serialized. Written up in `docs/findings/2026-09-02-bronze-is-single-threaded.md` **as a diagnosis, not a measurement** (§13): the confirming observation — Spark UI task count per Bronze stage, predicted to be exactly 1 per hour-file — has not been taken. The fix, for a later scoped task with its own before-and-after: one `spark.read.text([24 paths])` instead of 24 calls, `event_hour` from `_metadata.file_path`, one per-day `replaceWhere` — which keeps recovery granularity unchanged, since the checkpoint is already day-grained. **Deliberately not applied mid-run**: restarting a healthy 65/92 run would discard ~3.5 h of completed work to chase an unmeasured improvement on a credit that expires Sep 24. |
| 2026-09-02 | Phase 2 burn — sixth real run: the retry fix fails identically, and defect #8 turns out to be a URI scheme, not a timing lag | Triggered `databricks_job.backfill` for real (run `156595311273405`); `databricks jobs get-run-output` on the failed task; `databricks fs ls --long` on the staging volume hours after the fact; read the Databricks volumes docs on URI schemes against the error string; TDD: three new unit tests in `tests/unit/test_spark_path.py`; `make lint` + `mypy --strict` + full suite (205 tests) | **The retry fix failed in exactly the same way, 67 seconds in, which is what finally identified the real defect.** Same `[PATH_NOT_FOUND] ... file:/Volumes/almanac_dbx/burn/staging/2025-07-01-0.json.gz`. Two facts killed the read-after-write-lag hypothesis outright: the deployed wheel demonstrably contained the retry (the traceback runs through `day.py:123` to `_land_hour` to `day.py:151`), and all 24 staged files were still sitting on the volume *hours* later — so no amount of waiting was ever going to make that path resolve. Root cause: **the `file:` scheme**. Databricks defines `file:/` as access relative to the driver's local root, resolved by the JVM's `LocalFileSystem`; a Unity Catalog volume must be handed to Spark as the bare `/Volumes/...` path so it routes through the UC filesystem (every Spark example in the volumes docs uses the bare path; `file:/` appears nowhere for volumes). The driver's *Python* process reaches `/Volumes` via the FUSE mount, which is why all 24 downloads succeeded and the files were provably there — Spark's JVM was looking somewhere else entirely. **Defect #7's own fix is what caused this**: `.as_uri()` was added for defect #5, when staging was genuinely local disk and a bare path wrongly resolved against `dbfs:/`; moving staging onto a volume inverted the requirement, two files apart, with no shared symbol for a type checker or a test to connect. Fixed with `almanac.burn.day.spark_path()` — bare under `/Volumes` (`is_relative_to`, not `startswith`, so a local `/Volumes_backup` isn't mistaken for the mount), `.as_uri()` otherwise — keeping both lessons instead of trading one for the other. The retry, `Settings.max_land_attempts` / `land_retry_seconds`, and its three tests were **removed**: written for a transient the evidence no longer supports, and keeping them would have embedded the wrong explanation in a load-bearing comment. Three new unit tests pin both branches and the lookalike-prefix case; the existing hostile-`fs.defaultFS` integration test still covers defect #5's branch end to end. **The process lesson, recorded because it cost a paid run:** the retry tested a hypothesis instead of establishing which component was failing — exactly the loop `CLAUDE.md` warns about. Reading the scheme in the error string against the vendor docs for that storage type was free and would have settled it before run 6. Defect #8 rewritten in `docs/findings/2026-09-02-burn-deploy-and-first-run-defects.md`. Seventh trigger pending a go-ahead. |
| 2026-09-02 | Phase 2 burn — **the Tier 3 backfill completed**, and the successful run reported FAILED | Triggered `databricks_job.backfill` (run `227270110474807`) on a green tree (`97400ba`, wheel verified to contain `spark_path`); watched 5 h to terminal state via per-day checkpoint polling; read `jobs get-run-output`; derived cost from `get-run` durations against `2026-09-01-azure-pricing.md`; TDD for defect #9 (`tests/unit/test_cli.py`) | **92 of 92 days, 341,060,851 Bronze rows, 341,060,789 clean, 0 quarantined, 165.987 GB gz, zero missing hours across 2,208 files.** Cost **$11.96** — 5.047 billed cluster-hours at $2.370/hr — against a $31.71 estimate: **38% of estimate, 6.5% of the $184 credit**. Per-day 197.5 s vs the calibration's 523.5 s, 2.65x faster *while also running Silver*, which the calibration did not: fetch fell 174.3 s → ~13 s (the `fetch_concurrency: 8` tuning) and 111 s of cluster setup amortised across 92 days instead of 1. Cost inherits the *assumed* 0.75 DBU/node-hour from the pricing doc (§13); everything else measured. **Defect #9, the opposite shape to every earlier one:** the run wrote all 92 days, printed its full JSON summary, then reported FAILED with `SystemExit: 0` — `sys.exit(main())` with `main()` returning 0, and Databricks runs a `spark_python_task` inside IPython, where `SystemExit` propagates as an exception regardless of code (the log carries IPython's own `To exit: use 'exit', 'quit', or Ctrl-D` warning). Succeeding while reporting failure is the safer direction but still wrong: an orchestrator, a retry policy, or a demo reads job status, not logs. Fixed with new `almanac.cli.run_cli(main)` — non-zero exits, zero returns normally — applied at all three entrypoints that can run as a job task (`backfill.py`, `photon_ab.py`, `gold/runner.py`; `photon_ab` had never run, so it would have reported FAILED on success too), and deliberately not to the three local-only scripts, same reasoning that left `build_silver_fixture.py` alone for defect #5. Four tests assert success does not raise and that 1/2/255 still exit with that code, so the fix cannot swallow a real failure. **Left open and explicitly undiagnosed:** 7 staging files (`2025-09-30` hours 3–9, ~600 MB) survived `_clear_downloads`, on the final day only. A write-after-clear race and delete-path consistency lag are both ruled out; cluster-shutdown truncation fits (it predicts last-day-only, which is what was seen) and a lazy `Path.glob()` mutated mid-iteration does not (it predicts scattered leftovers; 91 of 92 days cleared perfectly). **The one-word `list()` fix was deliberately not shipped** — that would repeat defect #8's exact mistake of committing a plausible fix for an unconfirmed cause. The findings doc names the observation that would settle it. |
| 2026-09-02 | Phase 2 Task 8 — throughput published per layer, closing the Bronze-only risk | Read run `227270110474807`'s printed `BackfillReport.summary` for per-layer wall time and `jobs get-run` `setup_duration`/`execution_duration` for billed cluster time; recomputed the report's own `gb_per_bronze_hour`/`gb_per_silver_hour` independently; composed the three layer rates as reciprocals against the run total | **Measured, and the carried-forward risk resolved in the opposite direction.** Whole run **32.89 GB gz per billed cluster-hour** (33.76 execution-only) for Bronze **+** Silver, against Phase 1's **13.84** for Bronze **alone** — **2.4x better while doing strictly more work**, so the §4.5 warning that "if the full pipeline is worse than Bronze-only, Tier 3 shrinks" never bound. Per layer: fetch **462.2** GB/cluster-h (7.3% of execution), Bronze **52.96** (63.8%), Silver **119.64** (28.2%), 0.7% unattributed. The accounting closes as a check, not a claim: the three layer rates compose as reciprocals to 34.01 GB/h against an actual 33.76, the gap being exactly the unattributed 128 s. Gain attributed to fetch parallelism (`fetch_concurrency: 8`; the calibration spent a third of billed time single-threaded on HTTP) plus 471 s of setup amortized over 92 days instead of 1 — both present, **decomposition not measured**. The calibration's 523.5 s "billed" is smaller than this run's setup alone, so the two may not count provisioning alike; the comparison is therefore given on both bases and is 2.4x either way. `2026-09-02-backfill-throughput-per-layer.md`. |
| 2026-09-02 | Phase 2 — is 0 quarantined of 341M rows a clean dataset or a dead ruleset? | Read `conf/sources/gharchive.yml` against `quality.py`; audited what every existing quality test actually asserts; added 6 violation cases + a baseline + a completeness guard to `tests/unit/test_quality.py`; `uv run pytest tests/unit/test_quality.py` | **A real gap, found and closed.** Every prior quality test proved the *mechanism* using rules defined in the test file; the one test touching the shipped ruleset asserted only that each rule **resolves**, discarding the result — so a rule that parsed, bound and never fired passed it. **No test proved any shipped rule had ever quarantined anything**, which made 0 quarantined unreadable: clean data and an inert ruleset produce the same number. Six cases now corrupt one Silver column each and assert the exact set of rules that trips; **all five reject rules fire**, 17 tests green. So the 0 is a fact about the data. **Carve-out, and it matters:** `created_at_not_future` (`created_at <= ingested_at`) cannot discriminate on a backfill at all — every Q3 2025 row is ~1 year older than `ingested_at`, so it fires only on a future-dated corrupt timestamp, never on the skew case it guards. **Four rules had power on this run, not five.** Also honest about reach: all five test *presence*, not correctness, so "no structurally malformed rows" is supported and "no bad data" is not. **Bonus, unasked for:** the 62-row bronze-to-clean gap is dedup, **1 duplicate in 5.5M** — the first measurement of §12 trap 4 on real data at scale, which Phase 0 explicitly could not test (non-adjacent hours) and Phase 1 covered only by unit test. `2026-09-02-zero-quarantined.md`. |
| 2026-09-02 | Phase 2 Task 9 — Photon A/B pre-flight: a stale deploy and a concurrent-arm race, both caught before spending | `databricks workspace export` on the deployed `scripts/photon_ab.py`; `uv build` + `databricks workspace import --overwrite` for the wheel and both scripts; `workspace get-status` for a stored-size check; read `databricks.tf`'s task blocks for `depends_on`; `terraform fmt`/`validate`/`plan`/`apply`; `databricks jobs get` against the live job; `jobs run-now` | **Two defects caught before a paid run, neither visible locally.** (1) **The deployed code was stale.** `photon_ab.py` on the workspace was still pre-`a37bc7d` — `sys.exit(main())` — so this job's *first successful* execution would have reported FAILED, defect #9 again, on a job that has never run and therefore has no prior green to compare against. Rebuilt the wheel (now carries `almanac/cli.py`) and re-uploaded it plus both scripts; deployed `photon_ab.py` verified to end `run_cli(main)`. Wheel verified by **stored size** (37,421 bytes, matching local exactly) because checksum-by-download is unavailable here: `workspace export --format RAW` is refused (`Invalid export request: format=RAW, directDownload=false`) and the plain export mangles binary, returning 41,494 bytes — 4,073 extra — and an unreadable zip. The upload path is fine; the *export* path is what corrupts, which is worth knowing before anyone trusts a downloaded artifact. Re-confirmed the recorded rule that the wheel must go via `workspace import --overwrite`, never `databricks sync` (`dist/.gitignore` is `*`, so sync skips exactly the file that matters). (2) **The two arms race on staging.** `arm_standard` and `arm_photon` carry **no `depends_on`** — verified against the live job, not just the config — so Databricks runs them concurrently, and both were pointed at the same `--staging-dir` while measuring the same calibration day. `fetch_hour` does not skip an existing file; it re-downloads and does `write_bytes`/`replace` on the same `2025-08-13-N.json.gz.part`, from two clusters, on a shared FUSE volume, while the other's Spark may be reading it. Likely presentation is a torn gzip read surfacing as `PATH_NOT_FOUND` — **indistinguishable at a glance from defect #8**, which cost five round trips. Fixed with a staging subdir per arm; `terraform plan` showed **0 to add, 1 to change, 0 to destroy**, touching only the two `--staging-dir` values, which also re-confirms committed config still matches live state. Quota re-checked live rather than assumed: two concurrent 5-VM clusters need 40 of 48 available vCPUs. Run `674441653017831` triggered. |
| 2026-09-02 | Phase 2 Task 9 — the Gold layer could not address cloud storage; A/B arm 1 died on it | Triggered `databricks_job.photon_ab` (run `674441653017831`); `databricks jobs get-run-output` on the failed task and read the full traceback for the almanac frames; reproduced `Path('abfss://...')` locally; swept every `argparse type=Path` across all entrypoints; `ruff`, `ruff format`, `mypy --strict` (66 files) | **Predicted before the run failed, then confirmed by the traceback rather than assumed.** `arm_standard` FAILED with `AnalysisException: Missing cloud file system scheme`, and the frames name the chain exactly: `photon_ab.py:44` `Path(ctx.paths.silver)` → `runner.py:73` `register_silver_sources` → `sources.py:26` `CREATE TABLE ... LOCATION`. **`pathlib` is the entire defect.** `Path('abfss://silver@acct/events')` collapses the `//` to `abfss:/silver@acct/events`, which is then **no longer absolute**, so the old `.resolve()` anchored it under the driver's cwd — two silent corruptions producing a plausible string and an external table that fails only at first read. **This is the third instance of one class**, after defect #7 (bare local path resolving against `fs.defaultFS`) and #8 (`file://` on a UC volume): each time, a path decision correct on local disk and silently wrong against cloud storage. `spark_path()` fixed where #8 bit, not the class. **Structurally uncatchable here**: every Gold test drives the CLI with `str(tmp_path)`, a local path where `Path` is the right type, so the suite, `mypy --strict` and CI are all blind to a bug whose only trigger is a URI appearing nowhere but Terraform. Fixed as a **type asymmetry, not a cast** — `warehouse` is `str` (may be a URI), `metastore` stays `Path` (Derby genuinely is a local file), and `table_location()` passes URIs through while still resolving relative local paths, preserving the invariant the old `.resolve()` existed for. Six tests, two load-bearing: the emitted SQL keeps `abfss://` intact, and `str(Path(uri)) != uri` is pinned for abfss/s3/gs so the `str` route cannot be read as style and reverted. **Class swept, not just the instance**: every remaining `type=Path` takes a real filesystem path (`/Volumes` FUSE staging/checkpoints/out, `/local_disk0` metastores, workspace project dirs) and none receives a lake URI. `arm_photon` was cancelled while still waiting for a cluster, having done no work; the same defect would have taken out the Gold run too, since it shares `register_silver_sources`. |
| 2026-09-03 | Phase 2 — a Gold job, so the models meet the real quarter | Read `process_day` to confirm what the backfill actually lands; `databricks workspace list`/`export` on the synced `src/`; wrote `scripts/gold.py` + `databricks_job.gold`; `terraform fmt`/`validate`; `make check` | **Found that Gold had never touched real data, and closed the gap.** `process_day` is fetch → Bronze → Silver and stops, and Terraform carried only `backfill` and `photon_ab` — so the $11.96 and 341,060,851 rows bought two layers **nothing downstream ever read**, with `dim_repo` / `fact_pull_request` / `agg_repo_daily` built only over 2,000-row fixtures. `scripts/gold.py` is a launch shim in the same shape as the other two and carries no logic, since `almanac.gold.runner` is already a complete CLI. **The alternative was rejected on evidence**: pointing `python_file` at the synced `src/almanac/gold/runner.py` would execute a file whose imports resolve from the installed wheel — two copies of one contract — and the synced `src/` was verified **stale** (still `sys.exit(main())`, no `cli.py`), which is exactly that hazard already realised. Metastore deliberately ephemeral on `/local_disk0`, and correct rather than merely convenient: a **first** full build has nothing to be incremental from. It is **not** correct for a second run — `dim_repo` is a dbt snapshot, so SCD2's multi-version path needs catalog state outliving the cluster, and choosing that store (Unity Catalog vs Derby-over-FUSE, still unverified for file locking) is **its own decision, recorded as open rather than defaulted into**. Also generalised defect #9 from three fixed call sites into a test: every script a `spark_python_task` names must exit through `run_cli`, asserted against source, because the defect lives in the line that never runs under test — `photon_ab.py` carried `sys.exit(main())` unnoticed precisely because it had never run as a job. |
| 2026-09-03 | Phase 2 Task 9 — the A/B's dbt step pointed at a relative project dir; caught pre-flight | Read `_run`'s `GoldTarget` construction against `cli_flags`'s defaults; cancelled run `488000457742954` while both arms were still `PENDING`; 3 new tests in `tests/unit/test_gold_runner.py`; `terraform apply`; `databricks jobs get` to verify live params; `databricks external-locations list` | **Second certain failure caught before it cost anything.** `photon_ab.py` built `GoldTarget` with only `warehouse` and `metastore`, so `project_dir` fell back to `DEFAULT_PROJECT_DIR` — the **relative** `Path("dbt")`. A job task's cwd is not the repo root, which this project established the expensive way as **defect #3** (`--source-config`'s relative default, `FileNotFoundError` before the first run read a byte — the reason Terraform passes that path explicitly). dbt would have failed to find `dbt_project.yml` *after* both arms redid all of Bronze and Silver. Cancelled while both arms were `PENDING`: **zero compute spent**. **A second footgun behind the first:** `profiles_dir` does **not** follow `project_dir` — setting only the latter leaves `--profiles-dir` on the same relative default — so both are now passed, with a test pinning that behaviour and a third asserting `DEFAULT_PROJECT_DIR` is relative so the reason stays legible. `--target-path` and `DBT_LOG_PATH` moved to `/local_disk0` for the same underlying cause: both default under `--project-dir`, a workspace path the driver cannot write to. Verified live before re-triggering, not just in config: `--project-dir → /Workspace/Shared/almanac/dbt`, `--target-path → /local_disk0/dbt_target`, `DBT_LOG_PATH` set, per-arm staging intact. **One UC worry checked and cleared rather than assumed**: `external-locations list` shows all four lake containers (bronze/silver/gold/features) registered against the `almanac-lake` storage credential, so `CREATE TABLE ... LOCATION 'abfss://...'` is addressable. The remaining unknown is recorded rather than guessed: `_session()` returns the cluster's live session and `dbt_session`'s `getOrCreate()` therefore **ignores** the warehouse/metastore configs — the trap `runner.py`'s own docstring names — which on a cluster with a real metastore may simply be fine. |
| 2026-09-03 | Phase 2 Task 9 — **the Photon A/B ran, both arms SUCCESS**, and its §8.2 hypothesis is falsified | Triggered `databricks_job.photon_ab` (run `693303490917119`); retrieved both `ArmMeasurement` JSONs from `/Volumes/almanac_dbx/burn/photon_ab/`; read `jobs get-run-output` for both dbt logs; `databricks tables list` on `almanac_dbx.silver`/`.gold` after the run | **Measured, and one row of three is not usable — which the catalog listing, not the run, revealed.** Both arms report `compressed_gb = 2.0124815106391907`, byte-identical, so the slice is genuinely common. Bronze **1.18x** (213.9→180.6 s), Silver **1.96x** (63.4→32.3 s), Gold 1.45x (60.9→42.1 s); both arms `PASS=23 WARN=0 ERROR=0`. **`hypothesis_holds` is False.** §8.2 predicted Bronze would be helped "little or nothing" because it is "almost all JSON parsing" — but Bronze measured above the 1.10 materiality threshold, **and the premise was wrong about the code**: `_land_bronze` is `read.text` + two literal columns + a Delta write and parses no JSON at all (that is Silver's `parse_events`). Recorded as falsified rather than reworded, per §13. **Gold's 1.45x is discarded, not quoted.** Both arms shared one Unity Catalog metastore concurrently: `register_silver_sources`'s `CREATE TABLE IF NOT EXISTS` let the photon arm win, so `silver.events` points at `photon_ab_photon/clean` and **the standard arm's dbt read the photon arm's data** while its own Silver output sat ignored; and `gold.*` are MANAGED tables both arms `merge`-ed into simultaneously. Bronze/Silver were cleanly isolated per arm and stand. **Not fixed in this commit** — a per-arm dbt schema changes what the arms write and so needs its own run to produce a comparable number; shipping the fix unmeasured is defect #8's mistake. **Cost verdict withheld by construction**: `dbus_consumed` is 0.0 in both arms (the job passes no `--dbus`), and since Photon's entire penalty *is* the DBU premium, a cost verdict computed from VM time alone would systematically favour Photon. Closing §8.2 needs billing-system DBU totals for task runs `1063658033177813`/`719412933113624`, then `compare --usd-per-dbu 0.30 --usd-per-node-hour 0.474 --num-nodes 5` (the measured `D4ds_v6` westus3 rate, not the docstring's 0.249 placeholder). **Four things cleared incidentally** — first Gold run outside a laptop/CI: the `abfss://` fix works end to end, the explicit `--project-dir` fix works, `getOrCreate()` **does** drop the warehouse/metastore configs as `runner.py` warns but is harmless against a real UC metastore (previously unverified, now observed), and `run_cli` reported SUCCESS on a normally-completing task — the exact case that used to report FAILED. `2026-09-03-photon-ab.md`. |
| 2026-09-03 | Phase 2 — **Gold built on the real quarter**, and the stale pointer that would have faked it | Dropped 6 stale UC objects via `databricks tables delete` (a metadata op, no compute); triggered `databricks_job.gold` (run `655249155948620`); read the dbt log; `databricks tables get` afterwards to confirm what was actually read | **PASS=23 WARN=0 ERROR=0 in 678.21 s of dbt, for $0.78** (382 s setup + 803 s execution = 0.329 cluster-hours) — 0.4% of the credit. First time `dim_repo` / `fact_pull_request` / `agg_repo_daily` met anything but the 2,000-row fixtures, since `process_day` lands Bronze+Silver only. **Three tests are the project's premises, not dbt hygiene**, and all now hold at 341M-row scale: `assert_label_point_in_time` (no label computed from anything after the prediction point — the property the project exists for), `assert_label_exclusion_accounts_for_every_row` (conservation, the Gold-boundary echo of Silver's `clean + quarantined == input`), and `relationships_fact_pull_request_repo_id__repo_id__ref_dim_repo_` (referential integrity across the real repo population). Enforced `not_null` contracts passed too, so a wrong output *type* at scale would have reddened the build. **The near-miss is the finding.** `register_silver_sources` uses `CREATE TABLE IF NOT EXISTS`, and `almanac_dbx.silver.events` already pointed at `photon_ab_photon/clean` — the A/B arm's **one-day 2 GB slice**. A "real quarter" build against it would have resolved to one day, built every model, **passed all 23 tests** (they assert invariants, not volume) and reported SUCCESS in an identical log. `gold.*` were MANAGED tables holding both arms' contaminated output that an `incremental`/`merge` build would have merged into rather than replaced. Dropped all six first; EXTERNAL `silver.*` lost only pointers, data untouched. **Verified after the run rather than assumed**: `silver.events -> abfss://silver@.../events/clean`, the 92-partition quarter. **Still unexercised**: SCD2's multi-version path — `dim_repo` is a dbt snapshot, a first build captures only initial versions, and Phase 0's 3,792 renames need a second run over a different window plus a metastore outliving the cluster (an open decision, not defaulted into). Generalisable: **`IF NOT EXISTS` at a metastore boundary is a silent read redirection, not a no-op, and it fails in the direction that looks like success** — the second time this shape cost something in two days. `2026-09-03-gold-on-the-real-quarter.md`. |
| 2026-09-03 | Phase 2 Task 9 — per-arm schemas, so the A/B's Gold row can be measured at all | Parameterised the Silver source schema (`runner.default_source_schema` + `dbt/models/sources.yml`) and the Gold target schema (`dbt/profiles.yml`) on env vars; per-arm values in `databricks.tf`; 4 new tests; a real local `dbt build` (145 s) to prove the templating still resolves to the plain defaults; `make check` (236 tests, was 233); `terraform apply`; `databricks jobs get` to verify live | **The fix states the schema name once, which is the point.** Run `693303490917119`'s Gold row was unusable because both arms shared `silver`/`gold` in one metastore concurrently. Isolation needs the *creating* side (`register_silver_sources`) and the *resolving* side (dbt's `source()`) to agree, and those are two different files — so both now read `ALMANAC_SILVER_SCHEMA`, with `ALMANAC_GOLD_SCHEMA` doing the same for the target schema, matching the pattern the `databricks` profile target already used. Disagreement would mean dbt resolving a table nothing registered. Terraform sets `ab_standard_*` / `ab_photon_*`, which isolates the arms from each other **and** keeps them clear of the real `silver`/`gold` the gold job owns — so a future A/B can no longer strand a stale pointer where Gold expects the quarter, which is exactly what cost this session a near-miss. Verified live rather than in config: the two clusters carry distinct env vars. Local/CI unaffected — the `env_var` defaults keep every non-cluster run on `silver`/`gold`, proven by a real dbt build rather than by reading the template. Re-run `325049271562727` triggered. |
| 2026-09-03 | Phase 2 Task 9 — **the A/B at n=3**, and a correction: "falsified" was over-claimed | Two more replicate runs (`325049271562727`, `236314776904974`); pulled both arm JSONs after each (the job overwrites the same two paths); aggregated with a range rule **fixed before replicate 3 was run**; computed a break-even DBU multiplier against the backfill's measured shape; rewrote `compare_arms` to take replicates; `make check` | **Two rows stand, one cannot be called, and the harness was reporting a verdict it could not support.** Speedups across 3 replicate pairs — Bronze **1.18 / 1.05 / 1.23** (mean 1.15), Silver **1.96 / 2.40 / 2.06** (mean **2.14**), Gold **1.45 / 1.29 / 1.42** (mean **1.38**); all six arms SUCCESS, all three pairs byte-identical on `compressed_gb`. Silver and Gold clear the 1.10 threshold on every replicate and are the defensible results. **Bronze is withheld**: its range straddles, and the mean clearing 1.10 is exactly the case the pre-committed range rule exists to refuse — switching to the mean after seeing the data is the move the rule prevents. **The reason is the noise floor**: re-running an *identical* arm spreads 2.3–29.9% (photon bronze 19.9%, standard gold 29.9%), so the ~15% Bronze effect is smaller than the noise of the arms measuring it. **Correction to the row above and to `2026-09-03-photon-ab.md`:** that document previously said the hypothesis was **falsified**, on run `693303490917119` alone. Run `325049271562727` computed the opposite verdict from the same code and slice. The honest statement is neither — **the measurement never had the power to support either**, and "refuted" is a stronger claim than "cannot tell". **Harness defect, this project's own threshold-vs-distribution mistake:** `hypothesis_holds` returned a `bool` from n=1, and `paid_for_itself` returned `true` from `dbus_consumed = 0.0` (cost collapses to VM time, and Photon's whole penalty *is* its DBU premium, so it systematically favoured Photon). Both now refuse the question — `compare_arms` takes replicates, verdicts may be `indeterminate`, `hypothesis_holds` is `bool | None`, `paid_for_itself` is `None` without DBU totals, and `_run_cost` averages so replicate count cannot inflate cost. **Decision: do not enable Photon.** The unanswerable cost question was inverted into a break-even DBU multiplier against the backfill's real shape — **1.55 (Bronze no help) / 1.96 (mean) / 2.16 (best case)** — which straddles Photon's ~2x multiplier, and keeps straddling (1.79–2.28) across a plausible 0.5–1.0 DBU/node-hour range, so it does not rest on that assumed input. **A wash, and the leg that would decide it is the leg that cannot be measured.** The real lever is Bronze's single-threaded gzip: 63.8% of execution, the layer Photon helps least, and fixing it would also shrink the layer that makes this question ambiguous. **Not buying more replicates** — the variance is confounded (arms run concurrently with 441 s vs 111 s provisioning), so more n buys precision on a biased estimate; controlling the overlap is the fix. |
| 2026-09-03 | Phase 2 Task 9 — wrap-up: reader-facing artifacts, §5.1's exclusion taxonomy, and two §13 items | Audited the plan's exit gate line by line rather than from memory; refreshed README (status block + Mermaid `class` line) and CLAUDE.md; added §5.1's `label_exclusion` taxonomy checked **against `fact_pull_request.sql`, not against STATUS's summary of it**; probed `system-schemas list` and `az consumption usage list` for DBU totals | **The audit found my own pending-list was wrong in both directions.** Missed: README line 15 still read *"7 of 9 tasks"* and CLAUDE.md *"Phase 2 in progress"*, the README diagram still had Gold as `todo`, §5.1 had **no** `label_exclusion` documentation at all (Task 5 deferred *two* things here and only the era-bound half had landed), and PR #9 is open and unmerged. Over-stated: **the SCD2 metastore decision is not a Phase 2 gate item** — the gate asks for SCD2 *verified* (rename closes the old row, one current row, case-only renames, double rename → three versions) and Task 3's tests satisfy it; running the multi-version path on real data at scale was an addition of mine and belongs to Phase 3+. **The taxonomy was written from the SQL and that caught an error**: `open_unobserved` fires on a null `opened_at`, not on "still open" as the summary implied — documented precisely, with the `right_censored` vs `closed_no_response` distinction spelled out because collapsing "we do not know yet" into "there is no answer" would corrupt the trainable population. Diagram: Gold → `done`; **`API` deliberately left `todo`** — the REST source is built and tested but never lands in Delta, so that edge is still aspirational and marking it done would be a false claim. **§13: one item closed, one qualified.** Adjacent-hour duplicate rate is **measured at last** — 62 in 341,060,851, 1 in 5.5M across 2,208 consecutive files, the trap-4 gap Phase 0 explicitly could not test. Photon's per-layer effect marked `[~]`: wall-clock measured, DBUs still open. **DBUs are blocked for two concrete reasons, now recorded rather than left as "not done yet":** `system.billing` is not exposed on this metastore and enabling it returns `User is not an account admin for Account`; and Azure Cost Management returns `Microsoft.Databricks` rows tagged with exactly the right key (`ClusterId`, `BillingOriginProduct: JOBS`) but with `usageQuantity` and `pretaxCost` **null**, still lagging. **A structural finding that outlives both:** "DBUs per layer" is not measurable at all — DBUs bill per cluster-hour and the three layers share one cluster sequentially, so any per-layer figure is the run total apportioned by wall-clock share, i.e. arithmetic over numbers already published, not a meter reading. |
| 2026-09-03 | Phase 2 — **§13's one assumed input measured**, and it corrects two published costs | Granted account admin (after a cloud-only Entra admin user was created to reach the console); `system-schemas list`; `databricks schemas list system`; queried `system.billing.list_prices` and `system.billing.usage` via the Serverless Starter Warehouse (started for this, **stopped after**); re-ran `photon_ab compare` with real `dbus_consumed` | **Measured: 1.101 DBU/node-hour** on the backfill (27.788 DBU / 25.235 node-hours) against an assumed **0.75** — the assumption was **47% low**. Rates confirmed: `PREMIUM_JOBS_COMPUTE` and `PREMIUM_JOBS_COMPUTE_(PHOTON)` both **$0.30/DBU**, so Photon's penalty is consumption, never rate. **§13's own caveat proved exactly right** — the Premium decision held across the sensitivity range and the absolute cost did not: **the backfill was $14.62, not $11.96 (+22%)**, Gold **$0.85, not $0.78**. Cross-checked from raw DBUs, not just through the rate ($6.28 VM + $8.34 DBU). Tier 3 stays 7.9% of the credit against a 40% cap, so no tiering decision moves. **DBU consumption is workload-shaped, not node-shaped** (backfill 1.101 vs Gold 0.883/node-hour) — a single figure must not be quoted across shapes, the same error as quoting Bronze-only throughput for the full pipeline. **The Photon verdict hardens from "a wash" to a measured loss.** Multiplier **2.297** (2.118 / 2.381 / 2.394 across replicates), and the earlier break-even table was **arithmetically wrong** — it used `V = $0.474` as the VM rate, but that is VM ($0.249) *plus assumed DBU*, double-counting DBUs into `V` while also charging them through `d`. Corrected break-even **1.31 / 1.54 / 1.65**; the measured multiplier clears every one, and so does the lowest single replicate. Harness on real data: `dbu_overhead 2.297`, `cost_off $0.40`, `cost_on $0.74`, **`paid_for_itself: False`** — 1.85x the cost for a 1.31x speedup. Both errors (inflated `V`, assumed ~2x multiplier) happened to **flatter** Photon, which is why the recommendation survived them. `hypothesis_holds` stays `None`: the cost question became answerable, the Bronze speedup did not, and the two claims stayed independent — which is what the harness rewrite was for. **Access cost two confident wrong diagnoses, both recorded**: `system.billing` was never disabled (every schema reports `MANAGED`; the earlier "only `ai` and `information_schema`" was a **permissions filter on a non-admin identity**), and the `#EXT#` UPN is Entra's internal representation of a guest, not a sign-in name, so no password could ever have worked. The real blocker was `AADSTS500200` — the tenant's only Global Administrator was a personal Microsoft account, which the account console refuses. **Still not measurable: per-layer DBUs** — one cluster, three sequential layers, so any per-layer figure is the run total apportioned by wall-clock share. `2026-09-03-measured-dbus.md`. |
| 2026-09-03 | Phase 3 — feature platform concretized, before any code | Brainstorming + `almanac-design-decision` against design doc §4.4/§3.1/§5/§9; live-checked Databricks Feature Engineering in Unity Catalog (`create_training_set`, `TIMESERIES` primary keys) against Feast, both as of 2026-09-03; confirmed the Terraform-managed infra (workspace + storage account, 341M-row Gold/Silver/Bronze intact) is still live — `terraform state list` and `az databricks workspace list` both checked directly | **Three decisions recorded in §4.4a, none of them code yet.** Scope is **offline-only**: §9's Phase 3 row omits the online store and the vector index (Phase 5), so no online infra gets built ahead of a Phase 4 serving endpoint to feed it. **The as-of join is hand-rolled** in a new `almanac.features`, not delegated to Databricks Feature Engineering's `create_training_set` or to Feast (which does not remove the need to write the join — it just adds a second system to operate one in) — consistent with every other load-bearing correctness mechanism in this project (SCD2, dedup, quality rules) being owned rather than bought, and specifically because point-in-time correctness is CLAUDE.md's one stated invariant. **Middle path taken**: every feature table still gets `ALTER TABLE ... ADD CONSTRAINT ... PRIMARY KEY(entity_col, event_time TIMESERIES)` after being built — pure DDL, no `databricks-feature-engineering` client dependency, confirmed live against Databricks' UC feature-tables doc — for governance/lineage only, not correctness. v1 feature groups scoped to §5's model, not a general framework: `author_activity`, `repo_activity` (Silver-native, not a read of `agg_repo_daily` — §3.1's peer-of-Gold boundary), `pr_static`; the spine is the PR-opened population re-derived from Silver rather than read from `fact_pull_request`, for the same reason. **Separately, and not part of this decision**: the user chose to leave Phase 2's Terraform stack live rather than run the standing `terraform destroy` rule, because the storage account holding the real 341M-row quarter shares one Terraform state with the compute and there is no `prevent_destroy`/export path yet — destroying now would force a paid re-burn after Sep 24 to get Phase 3 data back. Recorded here as an open item, not a Phase 3 design question: a lifecycle guard or a separated storage state is still needed before the standing rule can run safely again. |
| 2026-09-03 | Phase 3 plan written | `docs/plans/2026-09-03-phase-3-feature-platform-plan.md` written from §4.4a, 8 TDD tasks | **8 tasks, none of them run yet.** Planning found a real correctness subtlety before any code existed: `author_activity`'s `prior_merge_rate` cannot count a prior PR that has not closed yet as "not merged" — its outcome is unknown, not zero — so it needs a self-join keyed on each prior PR's own close time, not a running average over open-order. "Average response latency" is cut from v1 for a stated reason (would duplicate `int_pr_events.sql`'s multi-event-type classification for one signal). Also caught before writing any test: this codebase has no precedent anywhere for `unittest.mock`/`monkeypatch` (`GoldTarget.cli_flags()` and `RestSession`'s injectable `sleep`/`now` are both tested as real objects) — the plan's CLI test asserts on `argparse.Namespace` output directly instead, and exercises `main()` for real in the integration test. |
| 2026-09-03 | Phase 3 Task 1 — the PR-opened spine | `uv run ruff check`/`ruff format --check`/`mypy --strict` on the new files; `pytest tests/unit/test_features_spine.py -v` (1 test) | **Green, and the plan's own Step 1 test was wrong the first time it ran.** The draft asserted `result[0]["as_of_timestamp"] == datetime(2025, 8, 13, 9, tzinfo=UTC)` directly on a collected row and failed on this machine (EDT, not UTC) — exactly the pitfall `tests/helpers.py`'s `epoch_of` docstring already documents: PySpark returns a collected `Timestamp` naive in the driver's local timezone. Fixed before commit, using `epoch_of` instead of a direct comparison; the same fix was applied to the plan document itself so it stays accurate, and the same pattern (a dict keyed by a *collected* timestamp, indexed with a UTC literal) was found and fixed in Task 3's two not-yet-written tests while it was still cheap to fix — server-side `.where(F.col(...) == F.lit(...))` filtering instead, which carries no such ambiguity. `build_pr_opened_spine` itself needed no changes once the test was correct. |
| 2026-09-03 | Phase 3 Task 2 — the as-of join engine | `ruff check`/`ruff format --check`/`mypy --strict`; `pytest tests/unit/test_features_join.py -v` (3 tests) | **Green, and the plan's own three-cycle TDD design worked exactly as intended.** The ordinary case (latest prior row wins) passed on the first implementation; the boundary test then failed exactly as the plan predicted it would — `where(event_time < as_of_timestamp)` filtered the *only* candidate row and dropped the spine row entirely (0 rows returned, not 1 with a null value) — and the cold-start test named the same defect from the other direction (no match at all, or only future matches). Both fixed together with one change: a `_qualifies` boolean computed per candidate (`coalesce(event_time < as_of_timestamp, false)`), ranked so a qualifying row always outranks a non-qualifying one, and feature columns explicitly nulled when the row that won the ranking doesn't qualify — so every spine row survives the join, and a future-only or absent history reads as "unknown", never as a dropped row or a leaked value. |
| 2026-09-03 | Phase 3 Task 3 — the bot classifier and the v1 feature groups | `ruff check`/`ruff format --check`/`mypy --strict`; `pytest tests/unit/test_features_bot.py tests/unit/test_features_groups.py -v` (4 tests) | **Green, one collection-time defect and one worked-example test both caught real things.** `is_bot_column` agrees with `classify_bot` on every case, including the documented false positives (`robotframework`, `Abbott` both correctly `False`) — the third independent restatement of one regex, matching `test_bot_macro.py`'s existing proof that the Python and SQL versions already agree. `compute_repo_activity`'s cumulative counts and `compute_pr_static`'s open-time attributes both passed on the first correct test. **A real defect, unrelated to feature logic**: `_OPENED`/`_CLOSED` as module-level `Column` constants failed test collection outright (`F.col(...)` asserts an active `SparkContext`, which import time does not have) — fixed to `_opened()`/`_closed()` functions called inside each compute function. **The worked example held**: `compute_author_activity`'s self-join correctly gives PR3 a `prior_pr_count` of 1 and `prior_merge_rate` of 1.0, not 2 and 0.5 — PR2 opened before PR3 but had not closed yet, so its outcome is unknown and the self-join's `prior.closed_at < this.opened_at` condition correctly excludes it from both the count and the rate, rather than the running-average-over-open-order approach silently counting it as a non-merge. |
| 2026-09-03 | Phase 3 Task 4 — assembling the training set | `ruff check`/`ruff format --check`/`mypy --strict`; `pytest tests/unit/test_features_assemble.py -v` (1 test) | **Green on the first implementation, no fixes needed.** `assemble_training_set` chains `as_of_join` across `author_activity` (on `author_login`) and `repo_activity` (on `repo_id`), then joins `pr_static` directly on `(repo_id, pr_number)` since it carries no `event_time` to look up. Passing on the first try confirms the v1 feature set's column names were chosen without collisions (`prior_pr_count`/`prior_merge_rate` vs. `events_total_to_date`/`bot_events_to_date`/`prs_opened_to_date`/`bot_share_to_date` vs. `is_draft`/`is_bot_author`/`opened_day_of_week`/`opened_hour`) — noted in the plan as something a richer v2 feature set would need to revisit with an explicit prefix, not yet needed here. |
| 2026-09-03 | Phase 3 Task 5 — Unity Catalog registration SQL | `ruff check`/`ruff format --check`/`mypy --strict`; `pytest tests/unit/test_features_registration.py -v` (2 tests, no `spark` marker — pure string logic) | **Green, pure SQL-string building only.** `primary_key_sql` returns a `(drop, add)` pair rather than a single statement — the drop exists because Task 6's runner fully overwrites each table's data on every run, and without it a second run's `ADD CONSTRAINT` would collide with the first run's still-live one. A temporal table gets `PRIMARY KEY (entity_col, event_time TIMESERIES)`; `pr_static`, which carries no `event_time`, gets a plain composite key rather than being mislabelled as time-series. No `spark.sql` call anywhere in this module's tests — live execution against a real Unity Catalog metastore is explicitly deferred to Phase 3's cloud verification step, not claimed here. |
| 2026-09-03 | Phase 3 Task 6 — the runner and CLI | `ruff check`/`ruff format --check`/`mypy --strict`; `pytest tests/unit/test_features_runner_cli.py -v` (3 tests, no `spark` marker) | **Green, no logic defects — only lint (import sort, line length), all auto-fixed or trivially split.** `_build_parser` is tested directly on its `argparse.Namespace` output (`--register` defaults `False`, `--schema` defaults `"features"`, both required flags enforced via `SystemExit`) rather than mocking `main()`'s dispatch — this codebase has no precedent anywhere for `unittest.mock`/`monkeypatch` (`GoldTarget.cli_flags()` and `RestSession`'s injectable `sleep`/`now` are both tested as real objects), so this task didn't start one; Task 7's integration test exercises `main()` for real instead. `run_features` fully recomputes and overwrites each of the three v1 tables from Silver on every call — a stated, deferred limitation (not incremental), not a silent one. |
| 2026-09-03 | Phase 3 Task 7 — the leakage suite and a real runner integration test | `pytest tests/integration/test_features_leakage.py tests/integration/test_features_runner.py -v` (3 tests, real Delta I/O, no fixtures); full `make check` (whole repo, 259 tests, 1354.36s) | **Green, and the leakage test proves CLAUDE.md's governing invariant directly rather than by inference from the join's unit tests.** `test_features_join.py`'s boundary test proves a single query never lets a future row leak in; this test proves something the join test cannot: that *recomputing an already-built feature vector later* is only reproducible if the Delta version is pinned. A feature vector is built as-of `T` from version `v1`; a "late arrival" event (`event_time < T`, but appended as a real second Delta version well after `v1`, not backdated in place) lands as `v2`; re-querying `v1` reproduces the original result byte-for-byte, while re-querying live (`v2`) is still leak-free (`event_time < T` holds) but silently changes the answer — exactly why a training set pins its Delta version at build time rather than re-deriving "current" later. The runner integration test writes all three v1 tables from real Silver-shaped Delta input and confirms a second full run overwrites rather than duplicates (full recompute is idempotent by construction), and exercises `main()` for real — `SparkSession.getActiveSession()` picks up the test's own session, so no mocking was needed to prove the CLI wiring works. **One test-only defect found by the *whole-repo* `mypy src tests`, not the scoped per-task check**: `test_features_groups.py`'s `_row` helper had zero parameter annotations, invisible to Task 3's narrower `mypy --strict src/almanac/features/bot.py src/almanac/features/groups.py` because that scoped run never touched the test file — fixed with a `_Row` type alias, and the plan document updated to carry this fix so a future reader isn't surprised by it. Also re-confirmed, for the second time this project, that this project's `ruff format` reformats Python embedded in Markdown code fences — the plan document itself needed `ruff format` run directly against it before `make check` passed clean. |
| 2026-09-03 | Phase 3 Task 8 — wrap-up: exit gate, README, STATUS.md | Full `make check` (whole repo, 259 tests, 1331.63s), run after the README/STATUS.md edits below and before their commit | **Green — Phase 3's exit gate is fully satisfied**, all 8 tasks done. README's Mermaid diagram corrected to show `F` (Features) reading from `S` (Silver) directly, parallel to `G` (Gold), rather than downstream of it — the diagram had encoded the wrong dependency (`G --> F`) despite §3.1's peer-of-Gold rule being load-bearing since Task 3; caught only because this step re-read the diagram against the design doc rather than editing it from memory. Also marked `author_activity`/`repo_activity`/`pr_static` done and refreshed the README status block, which still read "there are still no features and no model" — an accuracy bug in a portfolio-facing document, left uncorrected would have stated something false about the repo's own current state. Two exit-gate items carried forward as explicitly open rather than silently closed: `terraform destroy` (from Phase 2), and live UC `TIMESERIES` registration, unit-tested as pure SQL-string-building only and never executed against the still-live Databricks target. |
| 2026-09-03 | Phase 4 — model + MLflow concretized, before any code | Brainstorming against design doc §5/§5.1/§6/§8.1/§9; live-checked MLflow's UC registry aliasing API, LightGBM/scikit-learn current PyPI versions, and the `databricks_model_serving` Terraform resource, all as of 2026-09-03; PR #10 (Phase 3) merged to `main` first (`b9de2cb`), `phase-4-plan` branched from the updated `main` | **Four decisions recorded in a new §5.2, none of them code yet.** The label (`time_to_first_response_seconds`/`label_exclusion`, computed only in Gold's `fact_pull_request.sql`) gets joined onto Phase 3's feature rows by a new training-set-assembly step outside `almanac/features/`, both sides pinned to matching Delta versions — a label is supervision, not a feature, so §3.1's peer-of-Gold rule doesn't have to extend the same "never touch Gold" boundary to it, only the same version-pinning reproducibility discipline Task 7's leakage suite already proved. **Regression on seconds, not classification**: §5.1's 5-category exclusion taxonomy was built for a continuous duration and is already tested; reframing as binary breach/no-breach would force an undocumented call on where `closed_no_response` falls. **Single-node training** (LightGBM 4.7.0 + a scikit-learn 1.9.0 baseline, both confirmed current today) over Spark MLlib, per §5.1's own "the model never needs the full firehose" argument — Spark's job stops at building the training set. **Serving is Databricks Model Serving's own REST endpoint invoked directly**, confirmed live against the current `databricks_model_serving` Terraform resource docs — no custom FastAPI service; `GET /features/{id}?as_of=` and `POST /similar-prs` are out of this plan's scope entirely. **A fifth, separate decision**: the real cloud verification step (training run, MLflow experiment, UC registration, the serving endpoint) runs now, before Sep 24, rather than in the "bounded paid window after Sep 24" §9 originally reasoned from a schedule Phase 3 finished 18 days ahead of — the trial credit doesn't expire for 21 more days, and running it now spends down credit that would otherwise be wasted instead of racing the deadline later. **Also closed a real gap found while starting this phase**: Phase 3 Task 8's plan called for its own STATUS.md verification-log row (Step 3) separate from the Next-section update (Step 4); only the latter was done at merge time. Added retroactively as its own commit (`1a8d609`) rather than left silently incomplete. |
| 2026-09-03 | Phase 4 Task 1 — the `ml` dependency group and the label join | `uv sync --all-extras --dev`; `pytest tests/unit/test_model_dataset.py` (1 test, real SparkSession); full `make check` (whole repo, 260 tests, 1323.85s) | **Green — but the plan's `ml` version floors needed two corrections before the group would resolve or run.** (1) **`pandas>=3.0.5` is unsatisfiable.** 3.0.5 is the current release, but `mlflow` 3.15.2 — and every 3.x — declares `pandas<3`, while `pyspark[sql]` 4.2 needs `pandas>=2.2.0`; the usable band is 2.x. Corrected to `pandas>=2.3.3,<3` (current 2.x) with a load-bearing-ceiling comment in the same register as the existing `pyspark<4.3` one. The plan checked "latest pandas" but not against the rest of this phase's own stack — the live-validation gap `almanac-design-decision`'s gate 2 exists for, and exactly the pandas-specific "real finding to fix and record" the plan's own Task 2 Step 5 anticipated. (2) **LightGBM 4.7.0 does not load on macOS out of the box** — `lib_lightgbm.dylib` links `@rpath/libomp.dylib`, absent from a stock system; `brew install libomp` (keg-only, 23.1.0) fixes it. Task 1 itself is unaffected (`join_label` is a pure Spark transform, its test imports no LightGBM) and the suite stayed green, but libomp is a prerequisite for Tasks 3–7's tests and is now installed on this machine — noted here, not a repo change, so a fresh checkout knows. `join_label` inner-joins Gold's `(repo_id, pr_number, time_to_first_response_seconds, label_exclusion)` onto the training frame, keeps only `label_exclusion IS NULL` rows (§5.1's trainable population), and drops the exclusion column. New package `almanac/model/`, deliberately outside `almanac/features/`: a label is supervision about the future outcome, not a point-in-time feature, so §3.1's never-read-Gold boundary does not extend to it (§5.2). |
| 2026-09-03 | Phase 4 Task 2 — `build_training_frame`, version-pinned end to end | `pytest tests/integration/test_model_dataset_versions.py` (real Delta I/O, real `run_features`, no fixtures); full `make check` (whole repo, 261 tests, 1320.03s) | **Green, after four corrections the plan's Task 2 did not anticipate.** (1) **`assert pinned == original` on `frame.to_dict("records")` cannot work.** The plan's single-event fixture puts the PR's own open event at its own as-of cutoff, so every temporal feature is a strict-`<` miss → NaN, and `nan != nan` makes a dict `==` compare unequal even for byte-identical frames. Switched to `pandas.testing.assert_frame_equal` — the right tool for "reproducible byte-for-byte", and it additionally verifies dtype, column order, and index. (2) **`mypy --strict` fails on `import pandas`** — 2.3.3 ships no `py.typed`; added `pandas-stubs>=2.3.3.260113,<2.4` to the dev group, pinned to track the pinned pandas 2.3.x. Precisely the stub finding Task 2 Step 5 flagged as possible. (3) **PLR0913** — `build_training_frame`'s 7 args (session + three paths + three independently-pinnable Delta versions) exceed ruff's default 5; added a `src/almanac/model/*.py` per-file-ignore extending the same "explicit keywords beat a config object" reasoning the repo already uses to waive it for test row-builders, and covering Task 7's runner. (4) **PLC0415** — the plan's mid-function `from almanac.features.runner import run_features` hoisted to module top, matching `test_features_runner.py`. `build_training_frame` reads Silver `/clean`, the three v1 feature tables, and `{gold_warehouse}/gold.db/fact_pull_request` each at its own optional `versionAsOf`, runs spine → `assemble_training_set` → `join_label`, and collects one pandas frame; the test proves a pinned build reproduces exactly after a later Gold append of a *different* PR's label lands as a second Delta version. |
| 2026-09-03 | Phase 4 Task 3 — the naive baseline | `pytest tests/unit/test_model_baseline.py` (2 tests); full `make check` (whole repo, 263 tests, 1346.32s) | **Green.** `fit_naive_baseline` computes the median `time_to_first_response_seconds` per `is_bot_author` segment; `NaiveBaseline.predict` maps each row's segment to its trained median and falls back to the overall median for a segment value unseen in training (§5.1's "baseline first, always" — the measured comparison every model must beat). One `mypy --strict`-driven change from the plan's sketch: `.median().to_dict()` types as `dict[Hashable, Any]`, not assignable to the `dict[bool, float]` field, so the dict is built explicitly as `{bool(segment): float(median) ...}` — which also normalises numpy bool/float keys to Python types, making `predict`'s `.map()` robust rather than dependent on pandas' key dtype. The first test was renamed to state what it asserts (each segment's own median, not the fallback the second test covers). |
| 2026-09-03 | Phase 4 Task 4 — `train_model`, LightGBM against the naive baseline | `pytest tests/unit/test_model_train.py` (3 tests, real LightGBM fits on synthetic data, no mock); full `make check` (whole repo, 268 tests, 1391.90s — one regression run covering Tasks 4 and 6, both additive; the Task 6 row below shares it) | **Green — the no-signal test passed first try, no flakiness the plan's Step 4 warned might need a coarser assertion.** `train_model` splits the frame, fits the baseline on the training split and LightGBM on `FEATURE_COLUMNS` (the ten model inputs, `.astype("float64")` so booleans and any future null convert cleanly / become LightGBM-native missing values), compares held-out MAE, and returns `beats_baseline = model_mae < baseline_mae` — §5.1's "no model ships without a measured comparison", enforced in code. Two changes from the plan's sketch: `**lgbm_params` typed `Any` not `object` (unpacking `dict[str, object]` into LightGBM's own typed `__init__` fails `mypy --strict` with six `arg-type` errors), and a new `[[tool.mypy.overrides]]` block for `sklearn.*` with `ignore_missing_imports` — scikit-learn 1.9 still ships no `py.typed` and has no maintained stub package; it is used only for `train_test_split` and `mean_absolute_error`, whose array/DataFrame arguments mypy still checks at the call site. `brew install libomp` (Task 1's finding) confirmed sufficient — the real LightGBM fits run locally. |
| 2026-09-03 | Phase 4 Task 6 — Unity Catalog registration, champion alias | `pytest tests/unit/test_model_registry.py` (2 tests); regression run shared with Task 4 above (268 tests, 1391.90s) | **Green.** `registered_model_name` composes the three-part `catalog.schema.model` UC name (pure string logic, unit-tested here). `register_champion` calls `mlflow.register_model` then `MlflowClient().set_registered_model_alias(alias="champion", ...)` — the current, non-deprecated aliasing API, confirmed against mlflow 3.15.2's live signature (`set_registered_model_alias(name, alias, version)`), and `from mlflow import MlflowClient` resolves. Its live execution against a real UC metastore is deferred to Task 9's cloud step — the same string-built-and-unit-tested / execution-deferred split Phase 3 Task 5 used for `primary_key_sql`. |
| 2026-09-03 | Phase 4 Task 5 — MLflow run logging | `pytest tests/unit/test_model_train_logging.py` (1 test, real local MLflow file-store, no mock); full `make check` (whole repo, 274 tests, 1366.17s — one regression run covering Tasks 5 and 7; the Task 7 row shares it) | **Green, after a real MLflow 3.x behaviour change the plan did not anticipate.** MLflow 3.x puts the local `file://` tracking store in "maintenance mode": `FileStore.__init__` **raises `MlflowException`** unless `MLFLOW_ALLOW_FILE_STORE=true` — which breaks the plan's whole "test against a real local `file://` store, never mock" strategy for Tasks 5 and 7. Set once in `tests/conftest.py`, with a comment: the tests exercise the logging *contract* against a hermetic per-`tmp_path` store; the real backend is Databricks (`--tracking-uri databricks`, wired in Task 8, exercised in Task 9); sqlite would only add an artifact-root to manage for zero test value. `log_training_run` logs `feature_columns`/`random_state` params, `model_mae`/`baseline_mae`/`beats_baseline` metrics, and the model via `mlflow.lightgbm.log_model(result.model, name="model")` — `name=`, not the deprecated `artifact_path=`, confirmed current in 3.15.2 — and returns `runs:/<id>/model`. |
| 2026-09-03 | Phase 4 Task 7 — the runner and CLI | `pytest tests/unit/test_model_runner_cli.py tests/integration/test_model_runner.py` (4 tests: pure `argparse.Namespace` + real Delta/MLflow, no mock); regression run shared with Task 5 above (274 tests, 1366.17s) | **Green.** `run_training` is Phase 4's I/O boundary in the same shape as `almanac/features/runner.py`: `build_training_frame → train_model → log_training_run → (if --register AND beats_baseline) register_champion` — §5.1's gate enforced in the wiring, not a checklist. `--register` off by default (needs a live UC metastore). `_active_or_local_session()` picks up a test's own SparkSession so `main()` runs for real with no mock. Three items beyond the plan's file list: (1) `test_model_runner.py`'s mid-function `run_features` import hoisted to module top (PLC0415, as in Task 2); (2) **`scripts/model.py` added to `test_cli.py`'s `JOB_ENTRYPOINTS`** — it is the `python_file` for Task 8's `databricks_job.train_model`, so the "every job entrypoint exits through `run_cli`, checked against source" conformance test (defect #9's generalisation) must cover it, and does; (3) `mlflow.search_runs`' `list[Run]`-or-`DataFrame` union return narrowed with `assert isinstance(runs, pd.DataFrame)` before `.iloc`. |
| 2026-09-03 | Phase 4 Task 8 — Terraform for the training job and serving endpoint | `terraform fmt` + `terraform validate` (`infra/terraform/`, provider `databricks` 1.130.0) — the plan's stated gate for this task; `terraform plan`/`apply` is Task 9's cloud step, not this one's. No Python changed, so Task 7's regression run still stands. | **Valid.** Adds `databricks_job.train_model` (single-node via `is_single_node = true` — the current mechanism; `num_workers = 0` + `spark.databricks.cluster.profile` is rejected under newer access modes), `databricks_model_serving.pr_review_sla_risk` (`served_entities` block, `workload_size` var, `scale_to_zero_enabled = true`), and a `databricks_catalog` / `databricks_schema` for the UC model registry (`force_destroy = false` — a registered model is state the credit teardown cycle must not drop, and unlike the lake there is no paid re-burn to recover it). `local.lake` gains a `features` tier (provisioned since Phase 0 but never in this map, because Phase 3 ran locally against fixtures). Two deviations from the plan's sketch: `model_serving_workers` (a `number`, unwireable to anything `databricks_model_serving` accepts) replaced by `model_serving_workload_size` (the `Small`/`Medium`/`Large` string the `served_entities` block actually takes); and `model_pip_dependencies` carries the corrected `pandas>=2.3.3,<3` from Task 1. **`entity_version = "1"` assumes Task 9's training run beats the baseline and registers v1 — if it does not, no version registers and `databricks_model_serving` cannot apply. That is a documented null result (§5.1), flagged for Task 9, not a bug to force around.** |
| 2026-09-04 | Phase 4 Task 9 (in progress) — `compute_author_activity`'s self-join is O(N²) per author, found at real scale | Three `databricks_job.build_features` runs over the real 341M-row Silver quarter (`924195167084181`, `48552036331781`, `485360890184517`), each hung in the same Delta write stage; a live executor thread dump and the AQE physical plan from the third; storage-account `Transactions` metrics over both hang windows. Then the fix: 3 new tests (RED on the burst case), reformulated `compute_author_activity`, `make check` (whole repo, 277 tests, 14:09), a rebuilt wheel, and a fourth `build_features` run. | **Root-caused and fixed — a Phase 3 bug only a real-scale run could surface. The fixed run then completed in 437 s of execution.** Task 9 needs the v1 feature tables materialised on the lake (Phase 3 ran offline against fixtures), so a `databricks_job.build_features` was added and run. It hung every time writing `author_activity`: ~10 of 17 write tasks finished in seconds, ~7 (the ones whose `author_login` hash held a bot login) never did. **Ruled out with evidence** — not Storage throttling (zero `ServerBusyError` on the account), not GC (7–10 s total), not OOM, not a lock (stuck threads `RUNNABLE`). **The cause**: the live thread dump showed the stuck threads spinning in `BytesToBytesMap.safeLookup` under a `HashAggregate`, and the AQE plan reported `Statistics(sizeInBytes=596.7 TiB, isRuntime=true)` on the `SortMergeJoin` feeding it — `compute_author_activity`'s `this ⋈ prior` self-join on `author_login` is a near-Cartesian blow-up for `dependabot[bot]` and friends, and `spark.sql.adaptive.skewJoin` cannot split a self-join. **The fix**: reformulated as a time-ordered running aggregate over each author's own open/close events (a resolution at each `closed_at`, a query at each `opened_at`, ordered so a query precedes a same-instant resolution — the strict `<`), read through `rowsBetween(unboundedPreceding, currentRow)`. O(N log N), skew-proof, the exact shape `compute_repo_activity` already uses. **One behaviour change toward correctness**: the old `GROUP BY (author_login, opened_at)` over the join counted each prior once per PR opened at that same timestamp — a bot opening K PRs in one second got `prior_pr_count = K × true` (rate unaffected). The new form counts once; pinned by a test that fails on the old code. Strict-boundary and per-author scoping unchanged, now each has its own test; the plan's original worked example is untouched and green. `docs/findings/2026-09-04-author-activity-self-join.md`. **The first two hung runs also carried a behaviour-neutral `cluster_log_conf`** (log delivery to `almanac_dbx.burn.cluster_logs`) added to the `build_features` and `train_model` job clusters — diagnostics before a fix, per the project's rule for an unattributable failure. Cost of the three cancelled feature builds: ≈$13 of the trial credit. |
| 2026-09-04 | Phase 4 Task 9 (in progress) — Gold's fact is a Unity Catalog table on the workspace, not a Delta path | The first training run (`961660350236501`) failed in `build_training_frame`; `databricks tables get almanac_dbx.gold.fact_pull_request`; the fix — `dataset.py`/`runner.py`/terraform + 4 test files re-pointed at a table name; `pytest` on the 6 model dataset/runner tests, then full `make check`. | **A local-vs-cloud divergence, same class as the backfill's `/local_disk0` and `file:` URI defects.** `[PATH_NOT_FOUND] abfss://gold@.../warehouse/gold.db/fact_pull_request` — `build_training_frame` read Gold's fact as path Delta, the layout the local Gold tests use. On the workspace the Gold job's dbt `session` run creates `gold.fact_pull_request` **unqualified**, and the cluster's Unity Catalog default catalog resolves that to the managed table `almanac_dbx.gold.fact_pull_request` in the metastore's own storage — not under the job's `--warehouse` abfss path, which is a dead parameter there (confirmed: the table is `MANAGED`, `storage_location` is the UC managed path, last written by the one successful Gold run). **Fix**: `build_training_frame` reads Gold's fact **by name** (`gold_table: str`, `spark.read.table(...)`, still `versionAsOf`-pinnable) — the more correct design, since a dbt model *is* a metastore table. Silver `/clean` and the three feature tables stay path reads: they are hand-written path Delta (`df.write...save(path)`), never registered. The model integration tests now register their tmp Delta dir as an external table so the by-name read is exercised for real, uniquely named per test (the `spark` fixture is session-scoped). `docs/findings/2026-09-04-gold-is-a-metastore-table-not-a-path.md`. Training re-run pending on the rebuilt wheel. |
| 2026-09-04 | Phase 4 Task 9 (in progress) — the same O(N²) bot skew, now in `as_of_join` itself | The re-run training job (`792359179723419`) ran 102 min then was cancelled; its live SQL plan; the `as_of_join` rewrite + 1 new test; `pytest` on the join/assemble/leakage/model suites, then full `make check`. | **The feature platform's core primitive had the same blow-up, and this one is the consequential fix.** With `compute_author_activity` fixed the feature-build ran clean (437 s), but the training job then hung in `build_training_frame` → `assemble_training_set` → `as_of_join(spine, author_activity, on=["author_login"])`. Live SQL plan: `SortMergeJoin(skew=true) LeftOuter` (spine 1.18E7 ⋈ author_activity 1.32E7 on `author_login`) → `WindowGroupLimit row_number()==1` → `Exchange Statistics(sizeInBytes=11.7 PiB, isRuntime=true)`. `as_of_join` was `spine.join(feature_table, on)` then `row_number().over(partitionBy(row_id))` — it materialises every (spine × feature) pair for a key before ranking throws all but one away, and `dependabot[bot]` carries ~10⁵ rows on **both** sides. **Fix**: union spine rows (query events at `as_of_timestamp`) and feature rows (value events at `event_time`), one timeline per `on` key, query sorted ahead of a value at an equal instant (the strict `<`), carry the latest value row forward with `last(struct(...), ignorenulls=True)` over `rowsBetween(unboundedPreceding, currentRow)` — the struct is taken whole, so a legitimately null feature value is kept, not skipped for an older non-null one. O(N log N), one shuffle-and-sort per key. `as_of_join`'s three existing cases (latest-prior-wins, strict boundary, cold start) and the leakage suite all stay green; new `test_one_key_with_many_spine_rows_and_many_feature_rows` pins the skewed-key shape. `docs/findings/2026-09-04-author-activity-self-join.md` (§"The same shape, again"). **Also in this commit**: `databricks_job.train_model` moves from `is_single_node` to the 5-VM shape — the run above spent ~40 min of single-node Spark on `build_training_frame` before the `as_of_join` blow-up even showed, so §5.2's "dataset.py's Spark reads are small" premise was wrong (the spine is a full Silver-quarter scan); the LightGBM fit still runs single-threaded on the driver, so §5.2's "training never distributes" still holds. Training re-run pending on the rebuilt wheel. Cancelled training run: ≈$0.30 (single node). |
| 2026-09-04 | Phase 4 Task 9 — **done: a real, measured null result, not a failure** | Training run `283014697409852` on the fixed, multi-node `train_model` job (12 min: 442 s setup + 288 s exec, vs. the single-node run's 102 min that never finished); `databricks api get` on the MLflow run for `model_mae`/`baseline_mae`/`beats_baseline`; `databricks registered-models get` confirming no version registered; a SQL query over `almanac_dbx.gold.fact_pull_request` (Serverless Starter Warehouse, started for this, stopped after) for the §5.2 SLA-threshold percentile. | **`beats_baseline = False`, measured, not forced.** `model_mae` **108,889.9 s** (≈30.25 h) vs `baseline_mae` **71,916.9 s** (≈19.98 h) — LightGBM with default hyperparameters is ~51% worse than the naive per-segment median. §5.1's own gate does exactly what it exists to do: `almanac_dbx.models.pr_review_sla_risk` does not exist (confirmed directly), so `databricks_model_serving.pr_review_sla_risk` is **not applied** — forcing it around a null result would be exactly the mistake the gate is there to prevent. **Diagnosed, not silently accepted**: the trainable population's `time_to_first_response_seconds` (n = 2,956,518, same `label_exclusion IS NULL` population) is severely right-skewed — p50 **72 s**, p75 **1,487 s**, p90 **76,533 s**, mean **72,054 s**, max **7,921,875 s** (91.7 days). `baseline_mae` sits almost exactly at the mean, consistent with MAE on this shape being decided by the multi-day tail; a default squared-error-style objective pulls LightGBM's predictions up for the ~50% of PRs that resolve in about a minute, which costs more in MAE across millions of fast rows than it recovers on the tail. Recorded as the measured explanation, not applied as a quiet fix — §5.1's "not retried until it flips" holds, and a log-transformed target or a different loss is a stated next iteration, not tonight's number. **§5.2's deferred SLA threshold, closed**: **p75 = 1,487 s (≈25 min)**, computed once from the real trainable population as committed, consumed by no code in this plan. **Provenance verified from the run itself**, not assumed: MLflow's `sparkDatasourceInfo` tag records the exact Delta version of every input read (`events/clean` v91, all three feature tables v0, Gold's fact v1) — the version-pinning discipline Task 7's leakage suite proved is visible end-to-end on a live run. `docs/findings/2026-09-04-model-serving-measured.md`. **Task 9 cost, full window**: three cancelled feature-build runs (≈$13) + one cancelled single-node training run (≈$0.30) + one successful feature build (≈$1) + one successful training run (≈$1) ≈ **$15 of ≈$150** remaining credit, 20 days before the Sep 24 expiry. |
| 2026-09-04 | Phase 4 — closing §5.2's deferred question: classification, before any code | Direct SQL against `almanac_dbx.gold.fact_pull_request` (Serverless Starter Warehouse, started for this, stopped after): the full `label_exclusion` breakdown, a check of the naive "every `closed_no_response` row is a breach" move before adopting it, and the exact breach rate overall and per `is_bot_author`; live-checked `LGBMClassifier` (current, 4.7.0) and MLflow's 2026 classification-evaluation guidance (PR-AUC over ROC-AUC on an imbalanced target) | **§5.3 written, and one naive move caught before it became a decision.** §5.2 chose regression specifically to avoid "an undocumented decision about where `closed_no_response` falls" — Task 9's measured null result is the trigger to answer it now, with real data. **The naive inclusion rule was wrong for 74% of the category**: of 4,363,603 `closed_no_response` rows, only 1,130,833 were open ≥1,487 s (the measured SLA threshold) before closing without response; 3,232,770 closed *faster* than the threshold, so the SLA window never had a chance to be exceeded — labelling all of them a breach would have overstated the positive class by 3.2M rows. Caught by running the check (Gate 1: "the cheapest disconfirming check... run it before concluding"), not by reasoning about it. **Decision**: trainable population becomes `label_exclusion IS NULL OR label_exclusion = 'closed_no_response'` (**7,320,121 rows, 2.48× the regression's 2,956,518**), `breach` derived at training time (no Gold/dbt change) with the two-branch rule above; `right_censored` and `author_unobserved` stay excluded (point-in-time correctness and no-anchor, respectively — both stated in §5.3); the SLA threshold is **reused** at 1,487 s (Task 9's measured p75), not recomputed on the wider population, to keep it a stable constant. **Measured breach rate 25.55% overall**, and **bots breach *more* than humans** (32.16% vs 21.97%) — the opposite of the pre-measurement intuition, stated because it corrected an assumption. Model: `LGBMClassifier`, same ten features. Baseline: per-segment breach *rate*, generalizing `fit_naive_baseline`'s existing shape (`agg` param, `"median"`/`"mean"`) rather than a second dataclass. Gate: PR-AUC (average precision) primary, per MLflow's live-checked 2026 guidance for a 25.55%-positive target; a **comparison sweep** of ≥2 `LGBMClassifier` configs (default, `is_unbalance=True`) logs every candidate to MLflow, registers whichever wins if it beats the baseline. Same registered model name `pr_review_sla_risk` — the regression run never registered anything, so there is no v1 to collide with. Phase 4 continues (Tasks 10–14, `docs/plans/2026-09-04-phase-4-classification-plan.md`); §5.2's regression result is **not deleted** — recorded, cross-referenced, and is the reason this section exists. `docs/design/2026-09-01-almanac-system-design.md` §5.3. No code changed in this commit. |
| 2026-09-04 | Phase 4 Task 10 — `join_breach_label`, and a shared feature-frame builder | `pytest tests/unit/test_model_dataset.py tests/integration/test_model_dataset_versions.py -v` (4 tests, 1 new unit + 1 new integration; both existing tests untouched and still green) | **Green on the first implementation.** `_build_feature_frame` extracted from `build_training_frame` (Silver → spine → `assemble_training_set`, no label) — `build_training_frame` itself is behaviourally unchanged, proven by its own unmodified test staying green. `join_breach_label` implements §5.3's population and derivation exactly: inner-joins `(repo_id, pr_number)` against `label_exclusion IS NULL OR label_exclusion = 'closed_no_response'`, deriving `breach` two ways (`time_to_first_response_seconds > threshold` for the trainable rows, `(closed_at - opened_at) >= threshold` for `closed_no_response`) — one test fixture covers both branches plus all three excluded categories (`author_unobserved`/`right_censored`/`draft`) in a single dict-equality assertion. `build_classification_frame` mirrors `build_training_frame`'s shape and version-pinning, proven end to end against real Delta I/O the same way Task 2's integration test proved the regression path. |
| 2026-09-04 | Phase 4 Task 11 — the breach-rate baseline | `pytest tests/unit/test_model_baseline.py tests/unit/test_model_train.py -q` (7 tests, 2 new; the two pre-existing median tests untouched and green) | **Green, and the generalisation is a real reduction of duplication, not a speculative one.** `fit_naive_baseline` gains `agg: Literal["median", "mean"] = "median"` rather than a second dataclass+function pair — median (regression, §5.1) and mean-of-a-boolean (classification breach rate, §5.3) are the same "segment → statistic → fallback to the overall statistic" shape, checked directly: nothing outside `baseline.py` touches `NaiveBaseline`'s field names, so renaming `medians`/`overall_median` to the generic `values`/`overall_value` is contained to this one file. Default `agg="median"` reproduces both pre-existing tests' results unchanged, proving `train.py`'s regression call site needed no change. |
| 2026-09-04 | Phase 4 Task 12 — `train_classifier`: the comparison sweep and the PR-AUC gate | `pytest tests/unit/test_model_train.py tests/unit/test_model_train_logging.py tests/unit/test_model_baseline.py tests/unit/test_model_dataset.py tests/integration/test_model_dataset_versions.py -q` (17 tests, 5 new; every pre-existing regression-path test untouched and green) | **Green, and a real methodological finding surfaced while writing the no-signal test.** `train_classifier` fits the breach-rate baseline and both `CLASSIFIER_CANDIDATES` (`"default"`, `"is_unbalance"`) on one split, scores each by `roc_auc`/`average_precision`/`log_loss`, and gates on the highest-`average_precision` candidate beating the baseline (`_best_candidate`, its own directly-tested pure function). `log_classification_run` logs every candidate plus the baseline as its own MLflow run in the same experiment. **Measured, not assumed**: a first no-signal test (`is_bot_author` held constant, matching the regression suite's existing fixture shape) asserted `beats_baseline is False` and failed — checked directly at n=400/1,000/2,000/5,000/10,000, and it stayed `True` even at 10,000 rows. Average precision rewards a model's *ranking variation*; a baseline predicting one tied value for the whole population (a single segment) has zero ranking power and is close to unbeatable-proof under PR-AUC regardless of true signal — a real property of the metric, not a bug. Giving `is_bot_author` genuine variety and asserting `roc_auc` near 0.5 instead (the metric that actually answers "was a real relationship learned") is the honest, robust test; forcing a specific seed/`n` to make `beats_baseline is False` pass would have been closer to p-hacking the test than testing the gate. `mypy` needed one narrowing (`np.asarray(...)` on `predict_proba`'s output before `[:, 1]`) for the same lightgbm-return-type reason Task 7's `assert isinstance(runs, pd.DataFrame)` existed. |
| 2026-09-04 | Phase 4 Task 13 — `run_training`'s `objective` dispatch, CLI wiring, classification integration coverage, and Terraform | `pytest tests/unit/test_model_runner_cli.py tests/integration/test_model_runner.py -q` (9 tests, 7 new; both pre-existing regression-path integration tests untouched and green); `terraform fmt -check`/`terraform validate` clean; whole-repo `pytest -q` (292 passed, 0:24:09), `ruff check`, `ruff format --check`, `mypy src tests` all clean | **Green, Task 13 complete.** `run_training` gains `objective: Literal["regression","classification"] = "regression"` and `threshold_seconds: int \| None`, returning `TrainResult \| ClassificationResult`; a guard clause raises before any Spark work if `objective="classification"` and `threshold_seconds` is `None`. **One deviation from the plan's sketch**: the plan called for `_build_parser` itself to enforce the cross-field rule, but argparse has no declarative way to express "required only if a sibling flag has value X" — added a small `_parse_args` wrapper (`_build_parser().parse_args()` + the one manual check + `parser.error(...)`) instead of subclassing `ArgumentParser`, matching this repo's composition-over-inheritance convention. Widening `run_training`'s return type surfaced a real `mypy` gap in the *existing* regression-path integration test (`.model_mae` accessed without narrowing the union) — fixed with `assert isinstance(result, TrainResult)`, the same narrowing precedent as Task 7's `assert isinstance(runs, pd.DataFrame)`. **Two new real integration tests** prove the classification path end to end through both `run_training` and `main()` against real Delta I/O and a real local MLflow file-store, no mock — a fixture note worth keeping: `join_breach_label`'s `closed_no_response` branch references `opened_at`/`closed_at` even on rows where `label_exclusion IS NULL`, because Spark compiles both `when` branches against the schema, so the classification fixture needs a wider Gold schema (`_GOLD_SCHEMA_WITH_TIMES`) than the regression one even though those columns go unused when every row's `label_exclusion` is `None`. **Terraform**: `databricks_job.train_model`'s `parameters` gain `--objective classification --threshold-seconds 1487` (the §5.3-measured constant, passed explicitly, never recomputed by the job) — same job, superseding what runs there rather than adding a parallel pipeline; `--catalog`/`--schema`/`--register` unchanged. Not yet `terraform apply`'d against the live workspace — that lands with the real cloud re-run, next. |
| 2026-09-04 | Phase 4 — classification cloud re-run, attempt 1: a real defect UC registration surfaced that no local test had (`register=True` had never fired for real before) | `terraform apply -target=databricks_job.train_model` (0 to add, 1 to change); `uv build` + `databricks workspace import --overwrite` for the wheel and `scripts/model.py`, verified by stored-size match (57,683 / 215 bytes, both exact); `databricks jobs run-now` (run `704688669520130`) — FAILED; `databricks jobs get-run-output` on the `train` task | **A real, paid failure, and a genuine first**: this is the first time `run_training(..., register=True)` has ever reached `register_champion` for real — Task 9's regression run measured `beats_baseline=False`, so `mlflow.register_model` was never actually called against a live Unity Catalog before this run (a candidate here *did* beat the breach-rate baseline, which is itself worth noting even though the run then failed on the registration step, not training). **Root cause, read directly off the real traceback**: `MlflowException: Model passed for registration did not contain any signature metadata. All models in the Unity Catalog must be logged with a model signature...`, raised by `UcModelRegistryStore._validate_model_signature`. Neither `log_training_run` nor `log_classification_run` ever passed `signature=` to `mlflow.lightgbm.log_model` — a latent bug in both paths since Task 6, invisible locally because a `file://` MLflow store does not validate signatures the way UC's registry does. **Fixed and closed at the level that would have caught it before spending anything**: `train_model`/`train_classifier` now compute `infer_signature(train_features, model.predict(train_features))` right where each model is fit and carry it on `TrainResult.signature` / `ClassifierCandidateResult.signature`; `log_training_run`/`log_classification_run` pass it through to `log_model`. Two new local regression tests (`tests/unit/test_model_train_logging.py`) assert `mlflow.models.get_model_info(model_uri).signature is not None` on every logged model — reproduced the exact real failure locally first (RED), confirmed the fix turns it green, so a future `register=True` run cannot hit this again undetected. `np.asarray(...)` needed once more around `model.predict(...)` for the same lightgbm-return-type `mypy` reason as Task 12. Whole-repo `pytest -q` (292 passed, 0:23:08), `ruff check`, `ruff format --check`, `mypy src tests` all clean. Wheel rebuilt and being redeployed; re-triggering now (attempt 2). |
| 2026-09-04 | Phase 4 — classification cloud re-run, attempt 2: **a real, measured, non-null result** | Wheel rebuilt (`uv build`) and redeployed (`databricks workspace import --overwrite`, verified by stored-size match, 57,895 bytes both sides); `databricks jobs run-now` (run `1102177434831425`) — SUCCESS; MLflow runs read via `databricks api get /api/2.0/mlflow/runs/get`; `databricks registered-models get` / `model-versions get` / the UC aliases API against the live workspace | **`beats_baseline = True` — the first non-null result this project's model layer has produced.** `default` (plain `LGBMClassifier`) beat the breach-rate baseline by more than 2x on PR-AUC: baseline `average_precision` 0.2845 vs. `default` 0.6120 (`roc_auc` 0.8279, `log_loss` 0.4219); `is_unbalance` close behind at 0.6074/0.8279/0.5081, not better. `_best_candidate` correctly picked `default`; `run_training`'s `register AND beats_baseline` gate fired for real for the first time and registered it. **Verified directly, not inferred**: `almanac_dbx.models.pr_review_sla_risk` version 1 is `READY`, sourced from the `default` candidate's run; the `@champion` alias resolves to that version with its metrics attached to the alias record. Full writeup, including the population this ran against (n=7,320,121, §5.3) and cost (≈$2 across both attempts, ≈14 min + ≈14.5 min on 5 VMs), in `docs/findings/2026-09-04-classification-model-serving-measured.md`. `databricks_model_serving.pr_review_sla_risk`'s Terraform resource now has a real version to point at (`entity_version = "1"`) but is **not yet applied** — standing up a live serving endpoint is being treated as its own decision, not implied by this run. Task 14 (wrap-up: exit gate, README, closing STATUS row) next. |
| 2026-09-04 | Phase 5 — embeddings + vector index concretized, before any code | Brainstorming + `almanac-design-decision` against design doc §8.3/§3.2/§4.4a/§5.3; live-checked Databricks Vector Search's metadata-filter syntax and its product rename to "Databricks AI Search" (`databricks-ai-search` now canonical on PyPI, `databricks-vectorsearch` a deprecated shim); PR #11 (Phase 4) merged to `main` first (`8b58d04`), `phase-5-plan` branched from the updated `main` | **Recorded in a new §8.3a, plus the 8-task plan (`docs/plans/2026-09-04-phase-5-embeddings-vector-index-plan.md`), no code yet.** §8.3's own sizing (~4.6M vectors at 100%) is named stale before anything else: it was a Phase 0 5%-sample projection taken before Tier 3 ever ran, and §5.3's own real query already counts 20.2M PR rows in the real quarter — Task 1 re-measures the real corpus rather than reusing that number, per this project's own Gate 1 discipline. **Point-in-time retrieval is named as a genuinely new primitive**, not a reuse of `as_of_join` (that answers "latest row before T"; a neighbor lookup answers "nearest candidates before T"), and brute-force nearest-neighbor at real scale would repeat the exact O(N²) self-join mistake this project already hit twice (`as_of_join`'s own original shape, `compute_author_activity`'s self-join) — the concrete argument for real ANN infrastructure, not just a governance preference. **Confirmed live**: Vector Search's metadata filters support range predicates on `TIMESTAMP` columns (`{"opened_at <": value}`), closing half of §13's open item on whether point-in-time filtering expresses cleanly; the other half, cost, is not published in Databricks' own docs, so Task 1 pulls the real SKU rate via `system.billing.list_prices` the same way Phase 2 resolved DBU rates, before choosing Vector Search over the FAISS fallback. A second leakage axis is named explicitly: a neighbor's breach outcome is only knowable once that neighbor itself closed, so an unresolved neighbor is null, not "no breach" — the same discipline `compute_author_activity` already applies to an unclosed prior PR. Success is downstream lift on the registered champion (0.612 PR-AUC) or an honest documented null, matching the phase table's own stated Phase 5 gate exactly. Scheduling mirrors §5.2's own precedent: the real cloud step runs now against the still-live ~$145 of the $184 credit, not deferred to a post-Sep-24 paid window as §9 originally planned. |
| 2026-09-04 | Phase 5 Task 1 — the real corpus, measured; the index-technology decision made from what could (and couldn't) be confirmed live | Two `get_json_object` counts against Bronze's real 341M-row `raw_json` (Serverless Starter Warehouse); `system.billing.list_prices` for the Vector Search SKU rate; Microsoft's live feature-region-support table for `westus3`; `databricks vector-search-endpoints list-endpoints` for real account access | **14,924,573 real candidate texts** (10,162,741 PR + 4,761,832 issue, non-null title+body) — **~3.2× §8.3's stale 4.6M-at-100% projection**, confirming Gate 1's warning that the old number measured a different, smaller thing than what now exists. At dimension 384, that is ~4 Vector Search units on a Standard endpoint, not the one-unit fit §8.3a speculated. **Region and account access both confirmed live and positive**: `westus3` supports AI Search (Microsoft's own region table, checked directly, not assumed from general availability); `list-endpoints` returns a clean empty list, not a permission error. **The SKU rate could not be resolved**: `system.billing.list_prices` returns zero rows for anything matching `VECTOR`/`SEARCH`, the first time this project has queried that table and found nothing — stated as an open gap (Azure may meter this outside Databricks' own SKU catalog), not papered over. **A minimal smoke-test endpoint was not created**: this session's own tool-permission classifier blocked an ad hoc `create-endpoint` CLI call as a new kind of billable, untracked resource — not worked around; the real creation is folded into Task 3 instead, Terraform-tracked, where it doubles as the real cost measurement §8.3's fallback rule actually needs. **Two corrections to §8.3a's own text, recorded here rather than edited in place**: `STORAGE_OPTIMIZED` *is* a supported `endpoint_type` (the CLI's own `--help` shows both values; §8.3a had hedged this as unverified), and the CLI marks the newer `ai-search` command group `*Public Preview*` while `vector-search-endpoints`/`vector-search-indexes` carry no such marker — so Task 3 targets the GA surface, not the preview rename, despite the PyPI-level rename being real. `docs/findings/2026-09-04-phase-5-corpus-and-index-choice.md`. |
| 2026-09-04 | Phase 5 Task 2 — embedding pipeline | `pytest tests/unit/test_embed_pipeline.py` (5 tests); `ruff check`/`ruff format --check`/`mypy --strict` on `src/almanac/embed/`; full suite `uv run pytest -q` three times before committing | **New `almanac.embed.pipeline`**: `extract_texts` reads Bronze's `raw_json` directly rather than Silver, because title/body never reach Silver's typed schema (Task 1's finding) — a deliberate, documented exception to §4.4a's Silver-native feature-platform rule, justified because point-in-time correctness depends only on event ordering, which Bronze carries just as authoritatively. `run_embedding_pipeline` is incremental: a `left_anti` join on `text_hash` against the existing embeddings table (via `DeltaTable.isDeltaTable`) skips content already embedded, since a PR's text never changes once Bronze lands it. **Real bug caught and fixed**: `spark.createDataFrame(pandas_df, schema=...)` maps columns by position, not name, and the preceding `left_anti` join reorders them (its `on` column moves first) — fixed by reselecting columns by name against the schema immediately before the call, with the reasoning left as a comment at the point it matters. **5/5 new unit tests green**, `ruff`/`mypy --strict` clean. **Gate 2 caught a stale secondary source**: a live-search claim that `sentence-transformers` was at 5.6.1 was wrong; PyPI's own JSON API (`pypi.org/pypi/sentence-transformers/json`, checked directly) showed 6.0.1 current, pinned in `pyproject.toml` with the correction noted inline. **One full-suite run intermittently reported 95 failed/188 passed/15 errors** the first time `torch`/`sentence-transformers` were imported at collection time alongside the suite's many real Spark sessions; re-running the single failing test in isolation passed clean, and two subsequent full-suite runs (919.38s and untimed) both came back **297 passed, 0 failed, 0 errors** — treated as environmental resource contention, not a regression, and not silently dismissed: recorded here so a recurrence has a prior data point. |
| 2026-09-04 | Phase 5 Task 3 — the Vector Search index, authored not yet applied | `terraform validate`/`terraform fmt -check` (`infra/terraform/`); live provider-schema lookup (`databricks/databricks` 1.130.0) for both new resources; live fetch of Databricks' own Delta-Sync prerequisites doc; PyPI JSON API for `databricks-ai-search`'s current version | **New `infra/terraform/vector_search.tf`**: `databricks_vector_search_endpoint` (`STANDARD`) and `databricks_vector_search_index` (`DELTA_SYNC`, `embedding_vector_columns` — pre-computed offline, not Databricks-managed `embedding_source_columns`). **Correction to Task 1's own findings doc**: the CLI's `--help` lists `STORAGE_OPTIMIZED` as a supported `endpoint_type`, but the Terraform provider's live schema (checked directly via the registry, not the CLI) currently accepts only `STANDARD` — moot for this decision (`STANDARD` is what was already chosen) but recorded so the two surfaces aren't assumed to agree. **A real prerequisite gap found before any real spend**: Databricks' own docs require Change Data Feed on a `STANDARD` endpoint's source table, and a bare `source_table` name implies Unity Catalog registration, neither of which `run_embedding_pipeline` did — fixed in the same commit: the write now carries `delta.enableChangeDataFeed=true`, and a new `register`/`schema` pair (reusing `features/registration.py`'s `register_feature_table` — the same external-table pattern, not a second one) exposes the embeddings Delta path as `almanac_dbx.embeddings.pr_issue_embeddings`. Both resources are authored but cannot `apply` yet — the index's `source_table` doesn't exist until Task 7's real pipeline run creates it, same "provision now, apply once the dependency is real" shape as `databricks_model_serving`'s `entity_version` in `databricks.tf`. `databricks-ai-search` (0.78, confirmed via `pypi.org/pypi/.../json`) added to the `ml` extra for Task 5's query-time client. One new unit test (`register=True` path, local Derby metastore) plus the existing 5 stay green. |
| 2026-09-04 | Phase 5 Task 4 — `pr_similarity`, retrieval's own point-in-time-correctness axis | `pytest tests/unit/test_features_similarity.py` (5 tests) + extended `tests/integration/test_features_leakage.py` (1 new test); `ruff`/`mypy --strict` on every touched file | **New `almanac.features.similarity`**: `compute_pr_similarity` queries Task 3's index once per spine row (`query_similar`, as_of passed to the index's own query, never filtered post-hoc — the same discipline `as_of_join` states), then joins each neighbor against `resolutions` (extracted from `model/dataset.py`'s breach-label CASE expression as a new shared `compute_breach_labels`, reused rather than restated) to compute `similar_prior_breach_rate` — **null, not 0.0, when every neighbor is unresolved as of the spine row's own as_of** (Spark's `F.avg` already ignores nulls, so this falls out for free rather than needing a special case), tested explicitly for the resolved, unresolved, future-neighbor, no-embedding, and issue-vs-PR-collision cases. **Two real bugs caught before they shipped**: (1) `createDataFrame` with an explicit schema maps `Row` objects by *position* too, not just pandas — verified directly (`Row(b=2, a=1)` against schema `[a, b]` came back `a=2, b=1`) — the same defect class Task 2 hit, now known to generalize; fixed with a plain tuple in schema-field order, comment at the point it matters. (2) A collected Spark `TIMESTAMP` is naive in the *driver JVM's* default timezone, not necessarily UTC — `tests/helpers.py`'s own `epoch_of` already documented this trap, and it was re-verified directly here (`TZ=America/New_York` collected a UTC noon instant as naive 08:00); fixed by deriving `as_of` from `F.unix_timestamp()` (governed by Spark's own session `timeZone`, explicitly `"UTC"`) rather than attaching `tzinfo=UTC` to the collected value. `FakeSimilarityIndex` (real brute-force nearest-neighbor + as_of filtering) moved to `tests/helpers.py`, shared rather than duplicated per file. |
| 2026-09-04 | Phase 5 Task 5 — `similar_prs`, demoed not served | `pytest tests/unit/test_embed_query.py` (3 tests); live Python-SDK-reference lookup for `AISearchClient`/`get_index`/`similarity_search`'s real signatures | **New `almanac.embed.query`**: `VectorSearchIndex` wraps the real `AISearchClient().get_index(...).similarity_search(...)` call (metadata filter `{"event_time <": as_of.isoformat()}`, confirmed live in Task 1/§8.3a) — deliberately not unit-tested beyond import, same "verified only against the real endpoint" precedent as `embed.pipeline.load_encoder`. `similar_prs(repo_id, pr_number, as_of, *, index, embeddings, k=10)` is §6's whole `POST /similar-prs` contract as a plain callable, per §5.2's no-service-yet precedent — looks up the PR's own embedding by `entity_key`, then reuses `query_similar`. **`databricks-ai-search` ships no `py.typed`**: a new `[[tool.mypy.overrides]]` entry added (same shape as the existing `sklearn.*` one), scoped comment explaining why. All 3 new tests reuse Task 4's `FakeSimilarityIndex` rather than a fourth fixture. |
| 2026-09-04 | Phase 5 Task 6 — downstream lift, wired for the real comparison | `pytest tests/unit/test_model_train.py` (9, 2 new) + `tests/unit/test_model_dataset.py` (3, 1 new) + `tests/integration/test_model_runner.py` (1 new); `ruff`/`mypy --strict` on every touched file | **`train_classifier` gains `feature_columns: list[str] \| None = None`** (defaults to the existing `FEATURE_COLUMNS`) and `ClassificationResult` now carries whatever columns a given run actually used, so `log_classification_run` logs the true list per run rather than always the module-level default — caught mid-edit that a `replace_all` had also rewritten `log_training_run`'s (the *regression* path's) unrelated `feature_columns` log call to reference a field `TrainResult` doesn't have; reverted before it became a real bug. New `SIMILARITY_FEATURE_COLUMNS` (`similar_neighbor_count`, `similar_prior_breach_rate`) is not folded into `FEATURE_COLUMNS` itself — whether it earns a place is this task's own question. **`build_classification_frame` gains `similarity_frame: DataFrame \| None = None`**, left-joined via new `join_similarity_features` (extracted for direct unit testing, same shape as `join_label`/`join_breach_label`) — absent, not zero-filled, when not given; proven both at the join level (a never-scored PR keeps its row, null) and at the full-function level (`without.columns` has no `similar_*` fields; `len(with_similarity) == len(without)`). **Synthetic-signal test proves the with-run can actually win**: base features pure noise, only a similarity column determines the label — the without-similarity arm scores near baseline, the with-similarity arm beats it, mirroring the classification plan's own no-signal-passes-first-try discipline one level up. No real comparison yet — that is Task 7's real run, against the registered champion's measured 0.612 PR-AUC. |
| 2026-09-04 | Phase 5 Task 7 (development) — distributed encoding, CLI, and the job, before the real run | `pytest tests/unit/test_embed_pipeline.py` (8, 2 new) + `tests/unit/test_embed_pipeline_cli.py` (3 new); `terraform validate`/`terraform fmt -check`; full suite `uv run pytest -q` (313 passed, 0 failed, 0 errors, 1166.47s) | **Found before any real spend, not after**: Task 1's own 151.1 texts/sec (Phase 0) applied to the real 14,924,573-text corpus is ~27h single-threaded — `run_embedding_pipeline`'s driver-collect design (Task 2) would make an "attended run" an unattended one. **New `run_embedding_pipeline_distributed`**: the encode step runs through `mapInPandas`, one `load_encoder` call per Spark partition rather than one already-loaded model shipped across the driver/executor boundary — parallel across this project's existing 4-worker job-cluster shape (16 vCPUs, `Standard_D4ds_v6`), an estimated ~1.7h for the real corpus. Shares `_pending_texts`/`_register_if_requested` with the existing `run_embedding_pipeline`, which stays as-is for its own directly-injectable-encoder testability — a real `mapInPandas` closure cannot take a test double the way a plain parameter can. **Tested at the boundary a real value would need Spark's own worker-process machinery to hide**: `_embed_partition`'s returned closure is called directly (not through real `mapInPandas`) with `load_encoder` monkeypatched, since that closure runs in the *same* process when invoked this way — proving the schema-by-name reselect and batching without a real model or real distribution; a second test proves the zero-pending path returns before `load_encoder` is ever called at all (monkeypatched to raise if it were). **A third occurrence of `_active_or_local_session`** (already duplicated once, in `features/runner.py` and `model/runner.py`) was about to become a fourth in this module's own new CLI — extracted to `almanac.spark.active_or_local_session`, both existing call sites updated, not left as a third copy. New `main`/`_build_parser` (run_cli-wrapped, same shape as every other job entrypoint) embeds both event types in one job run. **New `infra/terraform/embeddings.tf`**: `databricks_job.embeddings`, same cluster/diagnostics shape as `build_features`/`train_model`. Not yet run for real, and `databricks.tf`'s `train_model` job is deliberately untouched — Task 6's similarity-frame parameters only get wired in if the real comparison actually beats the champion, per §8.3a's own gate. |
| 2026-09-05 | Phase 5 Task 7 — Task 4/6 real-run scaffolding, and a real defect found on the embeddings job's first actual run | `pytest tests/unit/test_features_similarity_runner.py` (2 new) + `tests/integration/test_model_similarity_comparison.py` (1 new) + extended `tests/unit/test_embed_pipeline.py`/`test_embed_pipeline_cli.py`; real `databricks_job.embeddings` run `922451764451708`, cancelled after 4h42m; full suite green after the fix | **New `almanac.features.similarity_runner`** (`build_similarity_spine` + `run_similarity`, CLI) and **`almanac.model.similarity_comparison`** (`run_similarity_comparison`, gated on `CHAMPION_AVERAGE_PRECISION = 0.612`, both arms trained on the identical sampled population so the only difference is which columns the model sees) — new `infra/terraform/similarity.tf` (`databricks_job.pr_similarity`, `databricks_job.similarity_comparison`). **A real, cost-relevant defect found on the embeddings job's actual first run, not before**: Spark's own default split of the pending-text DataFrame produced **967 partitions**, and `_embed_partition`'s `mapInPandas` closure loads a fresh `SentenceTransformer` once per partition — 967 model constructions, each measured directly from the live driver log at 12–14 minutes (dominated by load overhead, not the ~100s of actual per-partition encode work), projecting the stage alone to ~52h. Cancelled at 274–277/967 (≈29%) done, **≈$11.15 estimated cost** (4h42m at the $2.370/hr rate Phase 2 measured for this exact cluster shape). **Fix**: `run_embedding_pipeline_distributed` gains `num_partitions: int = 16` and repartitions before `mapInPandas`, bounding model loads to a fixed, cluster-shape-matched count regardless of upstream partitioning; `_embed_partition` also loads its encoder lazily so a partition repartitioning leaves empty pays nothing. Pinned by a new test asserting a monkeypatched, call-counting `load_encoder` is invoked at most `num_partitions` times. `docs/findings/2026-09-05-embedding-partition-count-defect.md`. Re-run pending. |
| 2026-09-05 | Phase 5 Task 7 — the partition fix held; the re-run then hit a fork-time worker stall | Post-mortem of real run `630533628470951` (cancelled after 90 min execution): delivered driver `log4j`/`stacktrace.log` + executor `stderr` from the cluster-log volume; live web search on `TOKENIZERS_PARALLELISM` / `predict_batch_udf`; `pytest tests/unit/test_embed_pipeline.py` + `test_embed_pipeline_cli.py` (2 new); full suite | **The `repartition(16)` fix worked** — the encode stage (`ResultStage 7`) was submitted with exactly 16 tasks, one per slot, not 967. **Then every task froze.** `HangingTaskDetector`: `shuffle.read.recordsRead` stuck at 28,909 for task 14 from 14:03:55 on, `output.recordsWritten -> 0`; `MapInBatchEvaluator: Idle timeout ... No data received from the worker process ... isAlive = Some(true)`; task threads `RUNNABLE` blocked reading the Python pipe. A frozen read counter with a live silent worker is a **Python-worker stall**, not slow compute (28,909 texts is ~3 min of encode at Task 1's 151 texts/s; the worker produced nothing for 10-50×). **Proven: n=1, a worker-side stall. Provisional: the exact mechanism** — the signature matches the documented `tokenizers` (Rayon) / `torch` (OpenMP) threadpool-across-fork deadlock, but the one log that would name it (the per-task worker stderr) is node-local and died with the cluster. **Fix**: job cluster sets `TOKENIZERS_PARALLELISM=false` + `OMP_NUM_THREADS=1` (`OMP=1` is right on merit too — 16 single-thread workers on 16 vCPUs); `_embed_partition` sets both defensively and prints per-batch progress to stderr so the next run's logs are conclusive; new `--limit` / `embeddings_limit` for a bounded (~200K-text, <$1) proof run before the full corpus. `predict_batch_udf` (the current Spark-4.0 API for this, which would also drop the `repartition` shuffle) noted as the better path, deferred — the blocker is orthogonal to the UDF API and a second same-function rewrite mid-phase under a finite credit carries its own risk. `docs/findings/2026-09-05-embedding-worker-fork-deadlock.md`. ≈$3.85 this run, ≈$15 of trial credit spent reaching a first real embedding run, still no table. Re-run pending. |
| 2026-09-05 | Phase 5 Task 7 — the fork fix held end-to-end; the index is scoped to recent events | Proof run `572931281110752` (`--limit 200000`) to TERMINATED/SUCCESS; `az vm list-usage --location westus3` for GPU quota; per-day qualifying-text counts against real Bronze; `pytest tests/unit/test_embed_pipeline.py`/`test_embed_pipeline_cli.py` (2 new); full suite | **The tokenizers/OpenMP fork fix works.** The proof run's encode (`ResultStage 10`) ran exactly 16 tasks, all 16 completed (`finished in 1,192,187 ms`), `[embed]` progress flowing, zero freeze — 83.9 min, ≈$3.31, 400 K rows written + registered. **Throughput measured: ~168 texts/s aggregate** (~10.5/s per vCPU), so the full 14,924,573-text corpus is a **~25 h** CPU-bound encode — the old "~1.7 h" figure took Task 1's 151 texts/s as a per-core rate and ×16, a Gate-1 error now noted in the findings doc. **GPU is quota-blocked**: `az vm list-usage` shows every GPU VM family at 0/0 in `westus3`. CPU headroom is ~2.7× (Ddsv6 20→48 vCPU) → ~9 h / ≈$35. **Decision**: build the index over **recent events** — new `--since-date` / `embeddings_since` (Bronze `event_date` partition prune), `2025-09-20` measured at **~1.73 M** qualifying texts (~2.9 h, ≈$7). Every Phase 5 capability is demonstrated identically at this size; the full-corpus build is a one-parameter change plus a bigger cluster, the same shape as the roadmap's other deliberately-not-built extension points. `docs/findings/2026-09-05-embedding-worker-fork-deadlock.md`. **Scoped run `603111972146916`: TERMINATED/SUCCESS, 135.8 min, ≈$5.36, 1,459,551 new texts** — the fork fix held the whole run, both passes, no freeze. `almanac_dbx.embeddings.pr_issue_embeddings` now 1,859,551 rows (1.46 M scoped + the proof run's 0.4 M). ≈$24 cumulative embeddings spend. |
| 2026-09-06 | Phase 5 Tasks 4/6 — the real similarity run, and three bugs only real data could surface | Four real `databricks_job.pr_similarity` runs (`705047433065245` crashed, `647656767930289` and `26796189966426` inert, `1097688475035466` correct) + `databricks_job.similarity_comparison` run `754385014066975`; a live 6-variant filter probe and a 25-row neighbour probe against the deployed index; four read-only population measurements against Gold/Silver/the embeddings table; MLflow metrics and the UC registry read back directly; full suite `uv run pytest -q` (**326 passed, 0 failed, 1485.20s**) | **Three bugs, each hiding the next, none visible to `ruff`/`mypy`/the local suite.** (1) **Every issue embedding got a degenerate `issue:<repo_id>` key** — `extract_texts` read `$.payload.number`, correct for a PullRequestEvent but wrong for an IssuesEvent (`$.payload.issue.number`), and `concat_ws` silently drops the null: 718,086 of 1,859,551 rows (39%), **594,181 issue vectors lost to the collapse**, zero `pr:` keys affected. Fixed forward; `_parse_entity_key` also made total (returns `None`, never raises) so a malformed key counts but never resolves. The deployed index keeps the degenerate keys — recovery is a **re-key in place, not a re-embed** (Bronze join on `(text_hash, event_time, repo_id)` resolves 717,353, 99.90%), ≈$5 and ~2 h of encode saved. (2) **The spine reached back before the embedded window** — sampled the whole quarter against an index holding only `>= 2025-09-20`, and the point-in-time filter only ever returns *older* neighbours; fixed with `--since-date` wired to the same `var.embeddings_since`. (3) **The spine was read twice and `rand` is nondeterministic** — `compute_pr_similarity` collected the spine for its queries, then re-scanned it for the final join, so ~62,000 real neighbour pairs computed against sample A were joined against sample B, leaving **44 of 10,000 rows** (0.44%). Diagnosed by elimination, not by guessing the next plausible component: the join was measured healthy (62%), all six `event_time` filter encodings probed live and working, and 25/25 real spine rows returned a full k=10 — so `pairs` had to be populated, which the output itself confirmed (the empty-`pairs` branch nulls *every* rate; 30 were non-null). Expected coincidental overlap of two independent 10,000-row samples from 1,564,185 rows × the 62% embedding rate predicts **~40**; observed **44**. **Not reproduced locally** (n=1, simple `range` source — deterministic), so the conclusion is labelled as resting on elimination. Fixed by collecting once and building the join's keys from that same list; pinned by a test that *counts reads*, since a second evaluation is invisible whenever the samples agree. **Task 4's real output** (`1097688475035466`, 321 s + 840 s): 6,210 of 10,000 rows with neighbours = **62.10%**, matching the independently pre-measured 62% join rate to two decimals, `avg(similar_neighbor_count) = 6.21 = 0.621 × 10`, 4,785 non-null rates averaging 0.2657 — a partially-working run would not land on the predicted number. **Two recorded numbers corrected**: the "~34 ms/query from the cluster" figure divided 342 s by 10,000 spine rows when most issued no query at all (honest figure **~135 ms**, from 840 s ÷ ~6,210 real queries), and a first pass at Task 6's trainable population said 8.9% using the wrong predicate (`compute_breach_labels` also trains on `closed_no_response`; the real figure is **48.6%**, 760,715 of 1,564,165) — at 8.9% the fix would have been a ~2.7 h, ≈$6.50 job that was never necessary. **Task 6's result is neither the clean win nor the clean null §8.3a anticipated**: holding the candidate fixed, similarity is worth **+0.0484 AP (0.4848 → 0.5332, +10.0%)**, with ROC-AUC 0.8011 → 0.8198 and log-loss 0.3977 → 0.3791 moving the same way; best-of-arm is +0.0379 but mixes in a candidate switch, and `is_unbalance` got slightly *worse* (−0.0063). The identical `baseline` AP of **0.2172 in both arms** is the check that the comparison is genuinely paired. **`beats_champion = False`** (0.5332 < 0.612) and the registry confirms the gate fired rather than merely being computed — one version, `@champion` still on version 1, despite `--register` — so `train_model` does **not** get the similarity parameters wired in. Two caveats stated with the number: the 0.612 champion was measured on the full population and this on ~4,900 sampled rows, and the PR-dense index (bug 1) *favours* the similarity features, making +0.0484 an upper estimate rather than a floor. `docs/findings/2026-09-06-similarity-real-run.md`. |
| 2026-09-06 | Phase 5 Task 8 — wrap-up and exit gate | Every gate row walked against a real artifact, not a plan: Task 1's findings doc, Task 2/3/4's tests, the live `similar_prs` demo against `almanac-embeddings`, Task 6's two MLflow experiments and the UC registry; README Mermaid `V` node moved to `done` in this commit per the same-commit rule; full suite `uv run pytest -q` (326 passed, 0 failed, 1485.20s) | **Phase 5 complete — 8 of 8.** Exit gate, row by row: the index-technology decision came from a **real-measured 14,924,573-text corpus** (~3.2× §8.3's stale 5%-sample projection) rather than the stale number, with the SKU rate recorded as an honest open gap when `system.billing.list_prices` returned nothing. **Point-in-time filtering holds on both axes** — query-side candidacy is passed into the index's own filter (all six `event_time` encodings probed live) and label-side knowability keeps an unresolved neighbour null rather than "no breach"; the live `similar_prs` demo asserted every returned neighbour was strictly older than its query's `as_of`, on real data. **No brute-force path exists**: every real query goes through the ANN index, never a Spark self-join over the corpus. **The pipeline is incremental** (`left_anti` on `text_hash`), proven by the scoped run embedding 1,459,551 of ~1.73 M qualifying texts and skipping the proof run's overlap. **`POST /similar-prs` is demoed against the real live index** — three real PRs, k=5, and the first sampled PR's nearest neighbour is the immediately preceding PR in its own repo (`pr:998922291:1476` → `pr:998922291:1475`, 21 minutes earlier), with an unknown PR returning `[]`; the index/endpoint/vectors/query path are all real, the 3-row `embeddings` frame passed in is not the full Delta table, which is stated rather than glossed. **The champion gate held under its own terms**: `beats_champion = False`, and the registry read back one version with `@champion` still on version 1 despite `--register`, so nothing was promoted and `train_model` stays unwired. **Measured lift, honestly bounded** — +0.0484 AP like-for-like (+10.0%), ROC-AUC and log-loss agreeing, but on ~4,900 sampled rows against a champion measured on the full population, and on an index whose collapsed issue keys *favour* the similarity features. Neither the clean win nor the clean null the phase table anticipated, and recorded as exactly that. **Real spend this phase ≈$31** (≈$24 embeddings across two cancelled runs, a proof run and the scoped run; ≈$2.4 across four `pr_similarity` runs; ≈$0.6 the comparison), against the $184 credit expiring 2026-09-24. Deferred and unchanged from §8.3a: hybrid/lexical search and reranking, other embedding models, a standalone issue-dedup surface, GPU inference, the `@challenger` workflow, `GET /features/{id}?as_of=` as a service. One follow-up is named rather than silently carried: the 718,086 degenerate `issue:<repo_id>` embeddings are re-keyable in place via a Bronze join (99.90%) plus an index resync, ≈$5 and ~2 h cheaper than re-embedding. |
| 2026-09-06 | Vector Search's real idle cost, measured before teardown | `system.billing.usage` at hourly granularity joined to `system.billing.list_prices`, attributed per endpoint via `usage_metadata.endpoint_name`; endpoint/index metadata and a live `similar_prs` query captured before any destructive step | **A `STANDARD` Vector Search endpoint does not scale to zero, and the same billing SKU hides two opposite behaviours.** `almanac-embeddings` drew a flat **4.000 DBU/hour, every hour** — identical in the window where ~12,400 real queries ran and in the windows where nothing touched it. The Phase 4 model-serving endpoint bills under the *same* SKU (`PREMIUM_SERVERLESS_REAL_TIME_INFERENCE_US_WEST_3`) and drew DBUs only during its 2026-09-04 live window, then zero — so "serverless" says nothing about idle cost here, and the two must be reasoned about separately. Measured rate **96 DBU/day ≈ $6.72/day ≈ $202/month idle**, against $65.75 of total Databricks spend to date and a $184 credit expiring 2026-09-24: standing idle to expiry would have cost ≈$121, and to Phase 7's demo window (§9, Nov 9–22 — the next actual use, since Phase 6 needs the online store rather than this index) ≈$430. There is no measurable per-query charge; the hourly rate is the whole cost, which makes a bounded heavily-used window efficient and a long idle one the worst case. **Everything perishable captured first** in `docs/findings/2026-09-06-vector-search-live-state-and-teardown.md` — endpoint and index metadata (`indexed_row_count` 1,265,363, dim 384, DELTA_SYNC/TRIGGERED/HYBRID), and a live k=5 `similar_prs` trace byte-identical to one taken ~10 h earlier. Teardown destroys only the endpoint and its synced ANN copy: the 1,859,551-row embeddings Delta table (≈$24 of encode), both MLflow experiments, the registered champion and the 341M-row quarter all survive, so recreation is `terraform apply` plus one sync, never a re-embed. Noted with it: `vector_search.tf`'s `prevent_destroy` guard exists for a *different* failure (provider drift recreating the index on a plain apply) and must be restored after any deliberate teardown. |
